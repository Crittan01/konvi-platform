{
  "name": "@commerce/db",
  "version": "0.1.0",
  "private": true,
  "description": "Snapshot legacy de migraciones SQL (no canónico)",
  "scripts": {
    "test": "echo 'No tests for @commerce/db snapshot'"
  }
}
