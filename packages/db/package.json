{
  "name": "@commerce/db",
  "version": "0.1.0",
  "private": true,
  "description": "Base SQL schemas and Supabase logic",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1"
  }
}
