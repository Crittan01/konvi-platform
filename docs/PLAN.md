# PLAN — Plan maestro y backlog priorizado pre-producción

> Estado: VIGENTE · Última verificación contra código: 2026-08-13 @ develop (suite completa sobre working tree: **4143 passed / 23 skipped / 201 dbharness deselected**; coverage TOTAL 69.4% medida 2026-08-02, gate `COVERAGE_MIN=60`)

**Qué es este documento:** la fuente de verdad de "qué falta para producción y qué sigue". Se alimenta de la [auditoría consolidada 2026-08-02](../.audit/findings/2026-08-02-consolidated-audit.md) (IDs B/A/M con evidencia `archivo:línea`) y de los pendientes verificados de [`.context/04-next-steps.md`](../.context/04-next-steps.md). El producto que este plan lleva a producción está descrito en [`product/PRD.md`](product/PRD.md).

**Convención de IDs:** B1-B6 = bloqueantes de producción, A1-A13 = hallazgos ALTOS, M1-M20 = MEDIOS (el informe consolidado define 13 ALTOS; no existen A14-A20). Algunos pendientes de `.context` usan IDs propios (F1-F8, H7/H8, G-7, BLOQUE K/L) — se citan entre comillas para no colisionar con los IDs de la auditoría (p. ej. el "B4 anti-hibernation" de `.context` NO es el bloqueante B4).

**Escala de esfuerzo:** S < 1 día · M = 1-3 días · L > 3 días (estimación de ingeniería, no compromiso).

---

## A. Checklist Go-Live

Criterio de salida: **todas las filas en estado "Hecho" con su evidencia archivada**. Owner "Founder" = intervención humana fuera del repo; "Agente" = ejecutable en código/infra desde este repo.

| # | Ítem | Estado (verificado 2026-08-02) | Owner | Evidencia requerida |
|---|---|---|---|---|
| 1 | **B1 — Flip `AVEONLINE_GENERATE_REAL_GUIDES` a true** (founder-gate) | ✅ **CERRADO 2026-08-04 — guías reales ACTIVAS en prod.** UAT con guía real verde: **86732771636** (COORDINADORA, $7.530) generada y verificada `GENERADA` (`scripts/uat/runs/aveonline_guia_real_2026-08-03.md`). Flip aplicado y verificado vía Render API: `AVEONLINE_GENERATE_REAL_GUIDES=true` en konvi-api Y konvi-orchestrator + `real_guides_enabled=true` en `tenant_shipping_provider_config` (KAIU) — doble compuerta completa. **Pendiente founder:** anular la guía UAT 86732771636 en el panel Aveonline (la anulación NO existe vía API) | Founder (anulación manual guía UAT) + Agente (UAT ✅ + flip ✅) | ~~UAT~~ ✅ + ~~flip render.yaml~~ ✅ (verificado Render API) + anulación manual de la guía 86732771636 en panel Aveonline |
| 2 | **B2 — Rotación de credenciales (H7)** (founder-gate) | ◐ **P0 MUERTO 2026-08-19 — legacy `anon`/`service_role` DESACTIVADAS por el founder (Supabase → API Keys → disable JWT-based legacy keys) y verificado empíricamente:** `python3.11 .audit/check_old_creds.py` → **HTTP 401 en ambas** ("Legacy API keys are disabled", fechado 2026-08-19 por Supabase). Las keys filtradas en `be739a4` ya NO abren la DB productiva. Historia: keys viejas vivas verificado 2026-08-13 (riesgo aceptado documentado); rotación del resto de proveedores ejecutada antes. **Pendiente para cerrar la fila:** M19 (verify_token dev, runbook §2 paso 11). ~~Reset DB password~~ ✅ hecho por el founder 2026-08-19 (actualizado `.env.prd-backup`; runtime no la usa — PostgREST/Auth autentican por API keys — y el CLI local verificado operativo con `select 1` vía `supabase db query --linked`). G23 (retiro de fallbacks legacy en código) CERRADO por el agente el mismo día | Founder (legacy ✅ + DB password ✅ + M19) | ~~`check_old_creds.py` todo 401~~ ✅ 2026-08-19 + M19. H8 (`git filter-repo`) opcional (P3) |
| 3 | **B3 — Contrato legal tenant vs operador logístico real** (founder-gate) | ◐ **En curso.** Doc corregido 2026-08-02: `docs/legal/contract-template-tenant.md` (líneas contexto, 6.4 y 8) ya declara **Aveonline único** como operador logístico/subprocesador, alineado con su anexo (`docs/legal/subprocessors.md:21`; Envia fue eliminado del runtime en rev. 109). **Pendiente:** visto bueno de abogado antes de firma con tenants | Agente (redacción ✅) + Founder (abogado) | Contrato corregido (eliminado Envia de las 3 líneas) + visto bueno de abogado (solapa con acción 4 de B6) |
| 4 | **B4 — Re-certificación E2E conversacional** (founder-gate la parte live) | ◐ **Local VERDE 2026-08-03 con stack (15/15); la parte LIVE sigue founder-gate.** Con stack local (Supabase podman + connector :8000 + api :8001 + orchestrator :8002) y tenant sandbox `d0000000`: `UAT_TENANT_ID=d0000000-0000-0000-0000-000000000001 python3.11 scripts/uat/coherence_scenarios.py` → **15/15 escenarios verdes** (5m 25s, `gemini-3.1-flash-lite`; log `scripts/uat/runs/coherence_local_2026-08-03.md`) — el sync "BLOQUE K/L" quedó hecho ahí (los turnos responden el gate de método de pago; nueva assertion `check_asks_payment_method`; `pytest tests/test_a11_coherence_assertions.py` → 14 passed). El run encontró 1 bug real del bot: `payment_coherence` CASE B2 corrompe la pregunta contraentrega/online (repro aislada + 3 observaciones live en el log) — **corregido 2026-08-03** (commit `d01b2fd2`: el invariant trataba el DEFAULT de columna `payment_method='credit'` como elección explícita del cliente). Sin servicios (2026-08-02): `pytest tests/agentic/` 654 passed / 10 skipped; gate `--ci` 24 OK. Pendiente live (founder): corrida contra staging/prod con Aveonline real, que además cubre la entrega del link Wompi tras responder el método (en local la bloquea la ausencia de courier). 5 fixes de bot de agosto (#209-#213) sin re-certificación live | Founder (run live) | `python3.11 scripts/uat/coherence_scenarios.py` → 15/15 con bot LIVE (staging/prod; sync "BLOQUE K/L" ya hecho ✅) + `python3.11 -m pytest tests/test_a11_coherence_assertions.py` verde + log archivado en `scripts/uat/runs/` |
| 5 | **B5 — Cobertura de paths de dinero** | ✅ **Cerrado 2026-08-02.** `.coverage` del gate `--ci`: `wompi_webhook` 90.2%, `meli_webhook` 87.0%, `order_cancellation` 90.0%, `aveonline_client` 94.7% (medición previa: 53.7/37.7/36.6/45.7). Tests nuevos: `tests/test_wompi_webhook_money_paths.py`, `test_meli_webhook_processing.py`, `test_order_cancellation_pipeline.py`, `test_aveonline_client_money.py`, `test_uat20260720_money_path_integrity.py`. TOTAL 69.4% → gate sube a `COVERAGE_MIN=60` (M18) | Agente | Nuevo `.coverage` con meta por módulo alcanzada (≥80% en los 4 módulos de dinero ✓) + CI verde (gate `--ci` 24 OK / 0 ERROR ✓) |
| 6 | **B6 — Fase 0 fiscal founder** (founder-gate) | ☐ Pendiente. Declarada "ventana crítica 2-4 sem" el 2026-05-30 (ADR-0022, constraints y triggers SAS en `.context/04-next-steps.md` §"Fase 0 fiscal"); externa al repo | Founder | 6 acciones: contador SaaS contratado · facturación electrónica DIAN activa · pólizas E&O ≥$500M + Cyber ≥$500M · abogado revisó contrato tipo · nombre comercio Wompi = "KONVI" · autodiagnóstico exclusión IVA cloud |
| 7 | **Flip `MFA_MANDATORY_ENABLED`** (A1) | ☐ Pendiente. `false` en prod (`render.yaml:208-209`); write-roles operan sin MFA obligatorio. Rollout ya diseñado: setear `MFA_MANDATORY_START` a la fecha del flip (`render.yaml:212`) → notificar usuarios → `ENABLED=true` (gracia 14 días, `MFA_MANDATORY_GRACE_DAYS`). **Runbook ejecutable listo 2026-08-13:** [`docs/operations/runbooks/mfa-mandatory-rollout.md`](operations/runbooks/mfa-mandatory-rollout.md) — pasos día 0/X, verificación, rollback | Founder | Variable en `true` en Render + usuarios owner/manager enrolados (página `/dashboard/settings/security` ya existe) |
| 8 | **Verificación canario `cf-connecting-ip`** (A2, T4-01) | ✅ **CERRADO 2026-08-07 — NO spoofeable, con evidencia.** Cloudflare (edge de Render) **rechaza con 403 (error 1000) cualquier request que traiga `cf-connecting-ip` seteado por el cliente** — el header falso nunca llega a la app. Log canario del request normal: `resolved=190.252.x.x` (IP real); los 4 requests spoofeados (1.2.3.4 y la propia IP del allowlist MeLi) murieron en el edge sin generar log. Allowlist MeLi y rate-limits por IP se apoyan en valor no controlable por el cliente. Canario apagado y verificado (var eliminada, redeploy live, sonda post-cleanup sin línea canario). **Hallazgo colateral (bug real, corregido):** el webhook MeLi respondía 401 a llamadas sin JWT (usaba `Depends(get_service_client)`) — fix en `meli_webhook.py` + test de wiring | Agente | Evidencia: log `[XFF_CANARY]` + 403 edge Cloudflare; script `scratch/t4_01_xff_canary.py` |
| 9 | **Revisión trimestral IPs MeLi** (A3) | ✅ **Verificada 2026-08-03 (empírica):** la doc oficial es SPA no scrapeable, así que se verificó contra prod: **0 tenants con MeLi conectado** (`tenant_integrations` sin provider meli) y **0 órdenes MeLi** — no hay tráfico que validar ni riesgo activo. Las 4 IPs del allowlist (`meli_webhook.py:80-85`, fuente oficial verificada 2026-04-28) quedan vigentes con override por `MELI_WEBHOOK_ALLOWED_IPS`. **Trigger de la próxima revisión: cuando el primer tenant conecte MeLi** — señal de monitoreo: log `meli_webhook.alert_threshold_exceeded` en Render | Agente | Este resultado + query de tenants MeLi en prod |
| 10 | **Tenants legacy con `active_provider='envia'`** (M12) | ✅ **Verificado 2026-08-03 en prod:** `select count(*) from tenant_shipping_provider_config where active_provider='envia'` → **0 filas**. El `DEFAULT 'envia'` en migraciones viejas es cosmético (ninguna fila lo usa; el único tenant tiene `aveonline` + `real_guides_enabled=true`) | Agente | Query prod ejecutada 2026-08-03: 0 filas |
| 11 | **Plantillas Meta (HSM) por tenant** | ☐ Pendiente (founder-gate) | Founder | Plantillas aprobadas en Meta Business Manager de cada tenant productivo; UI ya existe en `/dashboard/integrations/whatsapp?tab=plantillas` |
| 12 | **Wompi production keys** | ☐ Pendiente (founder-gate; aplica cuando un tenant pase a operativo) | Founder | Keys de producción en Render + 1 transacción real verificada con reconciliación 3 capas |
| 13 | **Aviso de privacidad publicado** | ☐ Pendiente (founder-gate) | Founder | Versión canónica del aviso publicada por tenant (template ya existe en `settings/legal`) |
| 14 | **Dirección de notificación judicial de KAIU** | ☐ Pendiente. Único campo legal que le falta al comprobante (ADR-0040) | Founder | Campo diligenciado en datos del tenant + comprobante v2 emitido con el campo |
| 15 | **SMTP propio (IH-SMTP)** | ☐ Pendiente. Hoy Resend con dominio del operador; bloquea F7-email (recovery dual-channel) | Founder | Dominio propio verificado en Resend (DNS) + `RESEND_FROM_EMAIL` actualizado |
| 16 | **Segregación de ambientes: prelaunch → dev real** (decisión founder 2026-08-03, reactiva la suspendida 2026-07-20) | ◐ **Parcial.** Hoy local+scripts corren contra `konvi-prod` con el guard `prelaunch` (`scripts/_env_guard.py`: corre, pero avisa por stderr); el riesgo aceptado está documentado (`docs/infra/environments.md` §0). **El día del lanzamiento real:** (1) `LAUNCHED=True` en `_env_guard.py` → prod fail-closed para scripts testing; (2) recrear `konvi-dev` cloud (org Free separada) con `scripts/db/replay_migrations_dev.sh` + `scripts/db/bootstrap_dev_sandbox.py` (~10 min); (3) exportar `KONVI_SAFE_REFS=<ref nuevo>` | Founder (crear org/proyecto Supabase) + Agente (replay + cutover) | Checklist de `docs/infra/environments.md` §1 "Estado transitorio" + §2.2 ejecutado + `env_guard` abortando (exit 2) contra prod + query local viendo solo el tenant sandbox |

---

## B. Backlog técnico priorizado

Los ítems del checklist (A1, A2, A3, M12) no se duplican aquí. Esfuerzo y dependencias son estimaciones; el "qué" y la evidencia vienen del informe de auditoría.

### P0 — Antes o inmediatamente después del go-live

| ID | Qué | Por qué importa | Esfuerzo | Dependencias |
|---|---|---|---|---|
| ✅ A4 | Guardrails del bot **fail-open** ante excepciones DB — **CERRADO 2026-08-02:** fail-closed para guardrails de dinero vía `FAIL_CLOSED_INVARIANTS` (`agentic/invariants/base.py:43`): un invariant de dinero que lanza escala a humano y el texto NO pasa (`base.py:124`; test `tests/agentic/test_a4_invariant_fail_closed.py`) | Las 3 capas anti-alucinación son promesa central del producto | M | — |
| ✅ A5 | Cascada LLM peor caso ~5 min vs heartbeat Render 120s — **CERRADO 2026-08-02:** presupuesto total por turno `LLM_CASCADE_DEADLINE_SECONDS=100` (< heartbeat 120s) en `llm_invoke.py`; aplicado en `agentic/agent.py:597` (test `tests/agentic/test_a5_cascade_deadline.py`) | Mensajes duplicados al cliente final = experiencia rota y riesgo anti-spam Meta | M-L | — |
| ✅ A6 | Rescate Claude **muerto** — **CERRADO 2026-08-02:** `llm_claude_rescue.py` eliminado de `services/api/lib/` y `services/ai-orchestrator/`; `anthropic` ausente de requirements y `render.yaml` (test `tests/agentic/test_a6_claude_rescue_removed.py`). **Ojo:** `llm_cascade.py` (orchestrator + copia byte-equal en `services/api/lib/`, paridad blindada por `tests/agentic/test_llm_cascade_parity.py`) **conserva el tier Claude opcional** del cascade: sin `ANTHROPIC_API_KEY` el tier se omite en runtime; borrarlo rompería la paridad byte-a-byte de la copia | Código muerto que simulaba una red de seguridad inexistente | S | — |
| ✅ A7 | Doc canónica stale — **CERRADO 2026-08-03 (verificación final):** `01-state.md` header revalidado (251 migraciones, 4.298 tests, 79 tablas, sección auditoría 2026-08-02) y `AGENTS.md` con versiones reales (Next 16.2.11, Aveonline, 0 menciones a gemini-2.5/Next 14 — verificado con grep). Sumado al cierre de A13 (09-bot-flowchart, 06/07-contracts) y la reescritura de `04-next-steps`, la capa canónica queda sincronizada | Los agentes operan con mapas falsos del sistema | M | — |
| ✅ A10 | Polling backup de tracking Aveonline ausente — **CERRADO 2026-08-02:** job `_aveonline_status_poll` en `services/ai-orchestrator/worker.py:3334+` (intervalo 1h, candidatas = guías reales >6h sin update vía webhook, batch 25) con `AVEONLINE_STATUS_POLL_ENABLED/INTERVAL_SECONDS/STALE_HOURS/BATCH` en `render.yaml:503-509` y `.env.example:200-203` | Envíos congelados sin alerta = reclamos y retracciones | M | — |
| ✅ A12 | `X-Tenant-Id` autodeclarado sin verificar en dual-auth — **CERRADO 2026-08-02 (audit trail):** cada llamada internal-secret deja fila en `api_security_events` (`internal_auth.py` `_audit_internal_call` → `observability.record_api_security_event`): tenant declarado, path, método, outcome, user-agent; secret válido sin `X-Tenant-Id` → 400 logueado. La verificación criptográfica estricta del tenant queda amarrada al refactor A0.2c (transición anotada en `render.yaml`) | Quien obtenga el secret actuaba como cualquier tenant sin dejar rastro; ahora deja rastro forense | M-L | Refactor A0.2c para la verificación estricta |
| ✅ A13 | Docs canónicas de runtime desactualizadas — **CERRADO 2026-08-02:** `.context/09-bot-flowchart.md` reescrito (path agentic único, verificado contra código @ `5fdad396`), `.context/06-contracts.md` corregido (§16 deduplicada; Envia solo como nota histórica rev.109), `.context/07-schema-canonical.md` regenerado | Misma raíz que A7, en los contratos runtime | S-M | A7 |

### P1 — Primeras 4 semanas post-go-live

| ID | Qué | Por qué importa | Esfuerzo | Dependencias |
|---|---|---|---|---|
| ✅ M1 | Badge `human_takeover` invisible en móvil — **CERRADO 2026-08-02:** badge en `apps/web/app/dashboard/bottom-nav.tsx` (mismo dato que el sidebar desktop — conversaciones en `human_takeover` sin archivar; aria-label con conteo) | El operador móvil no ve que un cliente espera humano | S | — |
| ✅ M2 | Registrar las rutas reales en el tree L1 — **CERRADO 2026-08-02:** `.context/00-product.md` rev. 7 (decisión formal registrada en §6): Promociones y Comprobantes (Ventas), Categorías (Productos), Seguridad/Salud/Legal/Retención/Cerrar cuenta (Configuración) | Drift entre L1 y navegación real; bloqueaba decisiones de módulos | S | Decisión formal de producto (regla L1) |
| ✅ M4 | Runbook de reconciliación Wompi — **CERRADO 2026-08-03 (ya existía, verificado):** `docs/operations/runbooks/wompi-payment-reconciliation.md` (validado contra docs oficiales 2026-06-26): cubre webhook totalmente perdido, orden estancada `pending_payment`, procedimiento paso a paso | Dinero: la recuperación ya no depende de memoria del founder | S | — |
| ✅ M5 | Error boundaries por ruta + tests de componente UI — **CERRADO 2026-08-02:** 7 `error.tsx` (`app/`, `dashboard/`, inbox, orders, shipping, catalog, metrics) + tests de componente Vitest `components/ui/motion.test.tsx` y `components/command-palette.test.tsx` (suite Vitest: 320 passed) | Una excepción de render tumbaba la pantalla entera | M-L | — |
| ✅ M6 | `fn_apply_retention` sin rama `audit_log` — **CERRADO 2026-08-07:** migración `20260807120000_fn_apply_retention_audit_log.sql` aplicada a prod (ledger 252). Rama con el patrón de `pii_access_log` (hard_delete por TTL, scoped por tenant, dry_run), guarda legal estructural: exige `enabled=TRUE` en los 2 niveles → la política sigue `enabled=false`, la rama nace **dormida** (se cierra la habilitación ficticia, no se activa el borrado). Probada en local (5 casos SQL verbatim) y en prod (`fn_apply_retention('audit_log', true)` → 0, 79 filas intactas). dbharness 201 verde + baseline regenerado | Si alguien habilitaba la política, la retención de auditoría no se aplicaba y nadie se enteraba | S-M | — |
| M7 | "A6.3" RLS GUC middleware + "A6.4" Vault RPC ownership sin implementar — **DIFERIDO con razón 2026-08-03:** esfuerzo L con diseño de sesión DB por request; el aislamiento actual tiene 3 barreras verificadas (lint AST 0 gaps con ratchet CI, RLS 79/79 tablas, dbharness RLS deny cross-tenant con Postgres real). Riesgo residual aceptado documentado en HANDOFF §seguridad multi-tenant. Abordar post-go-live en sesión dedicada con diseño propio | El aislamiento hoy depende de lint + filtros de aplicación, no de la sesión DB | L | Diseño de sesión DB por request (sesión dedicada) |
| ✅ M8 | Doble default de modelo Gemini divergente — **CERRADO 2026-08-02:** default unificado: `orchestrator.py` toma `DEFAULT_PRIMARY_MODEL` de `llm_invoke.py` (una sola fuente de verdad; test `tests/agentic/test_m8_orchestrator_model_default.py`) | Comportamiento distinto según el path de invocación | S | — |
| ✅ M9 | Paths absolutos `/home/ansible` en tests/scripts — **CERRADO 2026-08-13:** 255 archivos portables (240 `tests/` + 15 `scripts/`, `Path(__file__).resolve().parents[N]`), 0 ocurrencias restantes (grep), shims symlink eliminados de `ci.yml`. Suite local **4143 passed / 23 skipped / 201 dbharness deselected** (264s) + **CI verde** (run `31732756174`, 5/5 jobs — la suite corrió en CI sin shims por primera vez). En el camino se parcharon 2 advisories JS publicados ese día (GHSA-5p4m js-yaml→4.3.1, GHSA-2v37 nanoid→3.3.18, commit `996b5e95`) | Suite portable; cualquier contribuyente puede correrla | M | — |
| ✅ M10 | Post-venta fuera de ventana 24h: promesa de canal rota — **CERRADO 2026-08-02:** el bot ya no promete confirmación solo "por este chat" (`orchestrator.py:1048`, `payment_link_tool.py:459`): el comprobante va SIEMPRE por correo + por WhatsApp si la CSW sigue abierta (error 131047; test `tests/agentic/test_m10_orchestrator_channel_promise.py`) | Promesa rota al cliente en tracking/cambios tardíos | M | — |
| ✅ M11 | Flag `agentic_enabled` fail-closed ante error transitorio — **CERRADO 2026-08-02:** `_get_agentic_meta` con 1 retry + stale-ok del último valor cacheado (`agentic/dispatcher.py:81-133`); solo degrada a fail-closed si NUNCA se leyó valor (test `tests/agentic/test_m11_agentic_enabled_fail_closed.py`) | Un glitch de DB desactivaba el bot para todos los clientes a la vez | S-M | — |
| ✅ M13 | 2 ADRs con número 0023 duplicado; ADRs 0018/0019/0028 con Estado falso — **CERRADO 2026-08-02:** estados corregidos a IMPLEMENTADO con evidencia de código; índice `docs/adr/README.md` creado (colisión 0023 documentada, próximo número libre 0041) | Trazabilidad de decisiones rota | S | — |
| ✅ ENV-1 | **DEV local segregado: Supabase local (podman) + Studio GUI como target por defecto del desarrollo.** — **CERRADO 2026-08-03:** `.env` / `apps/web/.env.local` → `127.0.0.1:54321` (DB `:54322`, ref `konvi-platform`, keys demo del CLI); stack completo vía `supabase start`; seed `scripts/db/bootstrap_dev_sandbox.py` OK (tenant `KAIU Dev (sandbox)` + 2 owners GoTrue local); env_guard clasifica `dev-safe` (slug local neutro, `tests/test_env_guard.py`); `services/api` acepta http loopback en startup. Smoke: REST lista el sandbox, web `/login` 200, API `/health`+`/health/ready` 200. Rollback: respaldos `.pre-env1` (detalle y limitaciones: `docs/infra/environments.md` §2.1). **Complemento del checklist §A #16** (dev cloud se reactiva el día del lanzamiento real) | Un error destructivo en dev local nunca toca datos reales — antes local corría contra prod (riesgo aceptado `prelaunch`, `docs/infra/environments.md` §1) | S-M | Ninguna (no requiere crear proyecto cloud) |
| ✅ M14 | `/health/ready` devolvía `detail=str(exc)[:200]` sin auth — **CERRADO 2026-08-02:** detalle genérico hacia afuera ("dependencia no disponible"); error completo a logs + Sentry (`main.py:332-365`) | Fuga de detalle interno a Internet | S | — |
| ✅ M15 | Gaps de rate-limit en writes sensibles — **CERRADO 2026-08-02:** `RL_WRITE_DEFAULT` aplicado a los 12 endpoints del gap: expenses POST + `/{id}/reverse`, product_attribute_definitions (3), settings/maintenance/idempotency-cleanup, integrations/aveonline/* (6: webhook configure/rotate/delete + carriers put/delete/seed) | Superficie de abuso autenticada sin freno | M | — |

### P2 — Primer trimestre post-go-live

| ID | Qué | Por qué importa | Esfuerzo | Dependencias |
|---|---|---|---|---|
| M16 | Cliente Aveonline duplicado espejo (`services/api` + `services/ai-orchestrator`, 1176 líneas ×2) — **CONTROLADO 2026-08-03:** los espejos están idénticos (diff vacío) y existe guardarraíl `tests/agentic/test_aveonline_client_parity.py` (paridad byte-a-byte en CI) → el drift rompe CI, no pasa en silencio. Unificación a paquete compartido **diferida** (refactor L con riesgo pre-go-live; solapa la pregunta de diseño de M7/A12) | Todo fix de shipping se aplica dos veces, pero la paridad está garantizada por test | L | Extracción a paquete compartido (post-go-live) |
| ✅ M17 | Telegram: `setWebhook` manual por tenant; MeLi: refresh de tokens lazy — **CERRADO 2026-08-07:** (a) MeLi: nuevo endpoint interno `POST /api/v1/internal/meli/refresh-tokens` (dual-auth `require_internal_service` sin X-Tenant-Id, cap por ciclo, error de un tenant no rompe el ciclo) + job `meli_token_refresh` en el worker (cada 6h, `MELI_TOKEN_REFRESH_*` en render.yaml/.env.example) — el refresh_token rota ≥4 veces/día sin actividad, lejos del TTL de ~6 meses. 12 tests verdes. (b) Telegram: el setWebhook manual queda documentado como runbook en `docs/integrations/telegram.md` §"Manual por tenant" (cierre documental suficiente — es 1 paso por tenant al onboarding) | Operación manual no escala; tokens MeLi morían en silencio | M | — |
| ✅ M18 | Gate de cobertura vs comentarios que decían 70 — **CERRADO COMPLETO 2026-08-13:** 207 tests nuevos para los 3 routers menos cubiertos (`conversations` 36%→100%, `marketplace` 28%→100%, `integrations` 43%→99%; archivos `tests/test_m18_*_coverage.py`) → **TOTAL 69.5% → 72.7%** con suite completa (**4349 passed** / 0 failed). Gate `COVERAGE_MIN` sube 60 → **70** (`validate.sh:26`, `pyproject.toml:87` alineados; margen ~2.7 pts documentado). Fix de aislamiento propio: el test de mapeo de errores Aveonline usaba la instancia de módulo de colección → resuelve en ejecución (mismo patrón anti-colisión `integrations` del repo) | El gate mentía; solapa con B5 | S | B5 ✅ (hecho) |
| M19 | `verify_token` dev en claro + UUIDs de tenant hardcodeados en migración backfill — **BLOQUEADO por B2 (founder):** rotar `'konvi-dev-direct-2026'` (`20260622_whatsapp_model_b_backfill_konvi_dev.sql:11`) exige actualizar el verify_token en la consola Meta del tenant dev a la vez que en DB — sin esa coordinación el webhook del tenant dev queda roto. Se ejecuta junto con la rotación H7 (B2) — **procedimiento exacto: runbook `credential-rotation.md` §2 paso 11** | Higiene de secretos/identificadores en repo | S-M | B2 (founder) |
| ✅ M20 | Placeholders `services/worker`, `services/cron`, `connector-shopify`, `connector-mercadolibre` — **CERRADO 2026-08-03 (decisión: mantener documentados):** los 4 son README-placeholder autodocumentados ("PLACEHOLDER VACÍO", no se despliegan); documentados en TRD §arquitectura y BACKEND §mapa de servicios. El procesamiento async real vive en el worker del orchestrator (documentado). Shopify = fase 13 (PLAN §C) | Ruido estructural resuelto por documentación explícita | S | — |
| "G-7 self-service" | Reversión de pago self-service del bot (0 hits en `agentic/`): hoy la radica el operador desde Reclamos — human-in-the-loop, legalmente más sólido | Solo si producto decide automatizarla; requiere tool nuevo + `tools_subset` por estado FSM | M | Decisión de producto (diseño actual es deliberado) |
| "F2" | Tokenización completa de `document_number` con Vault (hoy hash + last4 aditivo, rev. 96) | PII fuerte (cédula) con seudonimización mejorada | M | "A6.4" (M7) |
| ✅ "F3" | Migración de `audit_log` legacy a `consent_audit_log` — **CERRADO 2026-08-07 (no aplica, verificado en prod):** las acciones en `audit_log` son de auditoría general (`created`, `purged`, `payment_link_created`, `status_changed`…) — **0 eventos de consentimiento**; `consent_audit_log` (494 filas) es una fuente disjunta por diseño (writers: sic_report, data_subject_request, contacts, meli_webhook, offboarding). La premisa de "doble fuente" no se confirma en los datos → no hay nada que migrar | Doble fuente de auditoría de consentimientos — no existe en los datos | S-M | — |
| "BLOQUE K/L" | Higiene de tests: harness `coherence_scenarios.py` desincronizado del gate de método de pago — ✅ **CERRADO (parte local) 2026-08-03:** los turnos responden contraentrega/online (nueva assertion `check_asks_payment_method`), reset por escenario determinista (doble pasada: purge-contact + full_delete), acciones `!ensure_shipping`/`!reset` para el sandbox sin Aveonline, seed de catálogo UAT; 15/15 verde contra stack local (`scripts/uat/runs/coherence_local_2026-08-03.md`). Lo que quede de ajuste para el run live (Aveonline real) se trata dentro del founder-gate de B4 | La red de regresión conversacional (B4) queda coja si no se sincroniza | M | B4 |
| "F7-email" | Recovery dual-channel por email | Recuperación de cuentas sin depender de un solo canal | S | SMTP propio (checklist #15) |
| "F7-full" | Cart abandonment proactivo (hoy F7-lite reactivo) | Recuperación de carritos = revenue del tenant | M | Plantilla Meta aprobada (checklist #11) |
| "F8" | Multimodal imagen (reusa base `meta_media.py` del audio) — **FUERA DEL CIERRE (feature nueva, no brecha):** scope de producto post-go-live; la plataforma certifica sin ella | Catálogo conversacional por foto | M | Decisión de producto post-go-live |
| Custom Access Token Hook | Claims custom vía hook de Supabase Auth (feature beta) | RBAC más limpio en JWT | M | Supabase GA del feature |

> **Nota ADR-0003 (F1-F7):** la tabla de follow-ups de `.context/04-next-steps.md` quedó limpiada 2026-08-02 (fila "ADR-0003 follow-ups F1, F4, F5, F6, F7" en su tabla §Verificado-resuelto). Verificado en código 2026-08-02: F1 (SAR printable HTML, `data_subject_request.py:407`), F4 (UI retención), F5 (reporte SIC, `sic_report.py`), F6 (detector rectificación, `orchestrator.py:326`) y F7 (click-wrap legal, `settings/legal`) **ya están implementados**. Solo quedan F2 y F3 (arriba).

### P3 — Oportunista / condicionado

| ID | Qué | Por qué importa | Esfuerzo | Dependencias |
|---|---|---|---|---|
| H8 | `git filter-repo` para purgar `.env` de TODA la historia git | Higiene profunda; innecesario si B2 rotó todo (secretos viejos inutilizables) | L | B2 hecho + coordinación de clones (destructivo, reescribe hashes) |
| ✅ "B4-infra" | Anti-hibernation ping — **OBSOLETO 2026-08-04 (verificado Render API):** los 4 servicios están en plan **Starter** (always-on, sin hibernación) — el ping solo aplica a plan Free. El mecanismo existe apagado (`ANTI_HIBERNATION_ENABLED` default false, `worker.py:215`, sin vars en render.yaml) y queda disponible si algún servicio baja a Free | Solo importaba en plan Free | S | — |
| "C3" | DR/backup de Supabase | Continuidad del negocio | M | Plan de Supabase que lo soporte |
| Tenant environments | Entornos dev/staging/prod por tenant para UAT sin data real | Escala de UAT multi-tenant | L | Trigger definido: 5+ tenants productivos o Platform Console |

---

## B.2. Gaps de auditoría full-stack (G1-G28 — verificados contra código 2026-08-13)

Origen: gap-analysis de los 7 findings de dominio (23-jul) contra este PLAN + código de hoy (`.audit/findings/gap-analysis-2026-08-13.md`, evidencia `archivo:línea` por gap). De 43 hallazgos en alcance: 10 ya cubiertos por este PLAN, 3 cerrados en código, **28 gaps únicos** → tabla siguiente. Ejecución inmediata 2026-08-13 (lotes A-G, commits de esa fecha): **14 cerrados, 2 parciales, 12 agendados con razón**.

### Cerrados 2026-08-13 (lotes A-G, verificación integrada)

| ID | Qué se hizo |
|---|---|
| ✅ G1 | PII en logs: `_mask_phone` aplicado en `orchestrator.py` (outbound), `order_status_tool.py`, `db_persistence.py` (6 callsites + dict solo claves/tipos); tests `test_pii_log_masking.py` |
| ✅ G2 | Body-cap gateway: middleware 413 en `main.py` (`MAX_REQUEST_BODY_BYTES`, default 2MB); 5 tests `test_api_body_cap.py` |
| ✅ G4 | `RL_CATALOG_AI_SUGGEST` (20/h tenant+user) en `/suggest-content` (patrón ai_agents); test de wiring |
| ✅ G6 | MFA fail-open del proxy ahora emite Sentry throttled (5 min) vía import dinámico; fail-open conservado y documentado |
| ✅ G7 | Cookie `mfa_recovery_session` borrada en las 3 vías de logout (nuevo route handler `POST /api/auth/signout` + server action) |
| ✅ G16 | 5 módulos muertos (738 LOC) borrados con sus tests (`dian_normalization`, `tools/dispatcher`, `tools/inbound_dispatcher`, `slot_extractors`, `guardrails`); sección viva de `test_doc_partial_and_queue_fairness.py` conservada |
| ✅ G15 (parcial) | `orchestrator.py`: import muerto guardrails + banner vacío eliminados; PII mask. **Queda:** imports lazy por ciclo (diseño aparte, con G12) |
| ✅ G17 | Labels "Envia" eliminados de UI (catalog ×2, contacts, shipping fallback → neutro) |
| ✅ G18 | Multimodal: tiers GA (`3.5-flash`/`3.1-flash-lite`, preview fuera); 404/"not found" salta tier sin consumir intentos (patrón `llm_embed`); paridad `llm_cascade` byte-equal api↔orchestrator preservada; 15 tests nuevos |
| ✅ G21 | `.env.example` embeddings → `gemini-embedding-2` (modelo y fallback) |
| ✅ G22 | pnpm 10.33.0 → **10.34.4** (CVE-2026-59196/59194): package.json, render.yaml ×4, ci.yml, TRD.md |
| ✅ G26 | `META_GRAPH_API_VERSION` (env, default v22.0) — una definición por servicio consumida por los 5 puntos; tests por servicio |
| ✅ G28 | Connector alineado al set core (fastapi 0.139.0, pydantic 2.13.4, supabase 2.31.0, sentry 2.65.0); 70 tests connector verdes; comentarios CI/conftest/manifest actualizados — **follow-up:** el leg `py-core` del CI quedó redundante (merge con `validate` candidato de simplificación) |
| ✅ P3 residual | "v21.0" corregido en README y `.agents/rules/04` (quedan solo referencias históricas en docs/_archive y tablas de drift, correctas) |
| ✅ G5 | CSP con nonce por request: `script-src 'nonce-…' 'strict-dynamic'` (sin `unsafe-inline`/`unsafe-eval` en prod) + `object-src/base-uri/form-action/frame-ancestors/worker-src` + `upgrade-insecure-requests` solo prod. `lib/csp.ts` + proxy (nonce en `x-nonce` al render, matcher cubre todas las páginas HTML) + nonce al script anti-FOUC del layout. Verificado live local: 27/27 scripts con nonce en /login, nonce header↔body coincide; 7 tests nuevos (16/16 con proxy) + build prod OK |

### Parciales (quedan con siguiente paso definido)

| ID | Hecho | Falta |
|---|---|---|
| ◐ G3 | Payment-link: **reuso de link vigente** (criterio espejo de `payment_link_tool._find_pending_order`, TTL 30 min, 0 llamada Wompi/0 insert) + `RL_WRITE_DEFAULT`; 4 tests | ~~Idempotency-Key pendiente~~ → **CERRADO 2026-08-13 (2ª pasada):** `begin/finalize/abort_idempotency` cableado (patrón create_order; hash por order_id+route; replay con `Idempotency-Replayed: true`; abort en 404/409/422/500) + 6 tests existentes ajustados (`MagicMock(headers={})`) + 3 tests nuevos de cableado. G3 queda ✅ completo |
| ◐ G9 | **Fase 1 cerrada 2026-08-13:** split del ciclo en 2 loops asyncio (`_INBOUND_JOBS` 4 jobs latencia-críticos / `_MAINTENANCE_JOBS` 17 crons) — un cron pesado ya no retrasa el inbound más allá de su propia I/O síncrona; heartbeat por loop; métrica **`inbound_lag_seconds`** (edad del pendiente más viejo) como alerta temprana; env `WORKER_MAINTENANCE_INTERVAL_SECONDS`; `_poll_cycle` preservado como contrato (tests verdes); 10 tests `test_worker_g9_loop_split.py` | Fase 2: paralelismo de inbound por tenant (semáforo; el CAS ya protege el claim) o proceso dedicado — **trigger: `inbound_lag_seconds` sostenido >5s o 5+ tenants activos** |

### Agendados (con razón y trigger)

| ID | Qué | Severidad | Razón / trigger |
|---|---|---|---|
| ~~G23~~ | **CERRADO 2026-08-19** (desbloqueado por B2 paso 10: legacy keys desactivadas a nivel Supabase). Fallbacks legacy `anon`/`service_role` retirados del código de los 4 servicios + web (auth.py, worker.py, server.py, db_persistence.py, config.py ×3, utils/supabase ×3, proxy.ts, confirm/route.ts) + scripts (~35 archivos, SECRET_KEY canónico) + render.yaml (5 entries legacy fuera) + `.env.example` + validate.sh + fixtures de tests renombrados; 3 tests del fallback eliminados. Precondición verificada vía Render API: `SUPABASE_SECRET_KEY` presente en los 4 servicios. Snapshot paridad actualizado (13/5/35/96 → 11/4/34/95). Suite 4405 + vitest 353 + tsc 0 + next build + certify_stg 18/18 verdes. **Único remanente [F]:** borrar las 5 vars legacy (`SUPABASE_SERVICE_ROLE_KEY` ×4 + `NEXT_PUBLIC_SUPABASE_ANON_KEY` web) del dashboard Render — valores ya inertes (keys desactivadas) | ~~P1~~ | — |
| G8 | Adjuntos inbox en bucket público `tenant-media` → bucket privado + signed URLs | P2 | **G8a CERRADO + APLICADO A PROD 2026-08-14:** el hard-delete de tenant NO cubría `inbox-attachments/{tenant_id}/%` (fuga PII post-erasure) → migración `20260814120000` aplicada a prod (ledger 253) + verificada en prod (función con 2 prefijos + fix, RPC funcional con tenant inexistente → 0). **Bug latente descubierto por el test:** el trigger `storage.protect_delete` exige `storage.allow_delete_query='true'` y la RPC original nunca lo fijaba → habría fallado en prod de haberse ejercitado; ahora la RPC lo fija LOCAL a su transacción. Tests: unitarios del script admin + dbharness con Postgres real (CI verde tras 4 iteraciones documentadas: baseline manual, UUID hex, trigger delete, set_config). **G8b CERRADO 2026-08-14 (4 fases):** (1) bucket privado `tenant-inbox-media` + RLS por tenant (migración `20260814130000`, CI verde con db-harness); (2) upload persiste `inbox-media://{path}` + chat firma al render (server action con verificación tenant + refresh) + api valida el esquema; (3) worker firma la URL (TTL 24h) justo antes de enviar a Meta — firma fallida → failed+ack sin llamar Meta (3 tests); (4) script one-shot `scripts/admin/migrate_inbox_media_private.py` para los adjuntos viejos (dry-run default, re-apunta messages.media_url). Catálogo/logo SIGUEN públicos (el bot los reenvía por WhatsApp continuamente). **Pendiente founder/agente con stack:** correr el script de migración en prod (dry-run → --apply) + UAT visual del inbox |
| G10/G11 | `packages/shared-py/` — **análisis ejecutado 2026-08-13: NO viable sin cambio de deploy** (los 3 servicios Python tienen `rootDir: services/<svc>` en render.yaml → un paquete fuera del rootDir no se instala en el build de Render; cambiarlo es riesgo de deploy que la decisión M16 difirió post-go-live). **Lo que SÍ se cerró:** guardarraíl anti-drift `tests/test_parity_shared_modules.py` — byte-paridad (llm_embed, carrier_capabilities) + paridad funcional por AST (vault_helper ×3, wompi_client subset, observability api⊆orchestrator-OTEL). **Drift real encontrado y corregido:** `void_transaction_sync` del orchestrator había divergido del canónico del api (ValueError vs RuntimeError, timeout, strip, logging) — propagado verbatim; el comentario citaba un `test_wompi_void.py` inexistente → ahora el guard real existe | P2 | Refactor shared-py queda diferido (decisión M16); el guard ya protege el drift |
| G12 | 5 monolitos (15.7K LOC): cortes de cohesión — **Cortes 1+2+3 CERRADOS 2026-08-13:** `wompi_webhook.py` 2664→**1159 LOC** (−56%): email-templates → `lib/email_templates.py` (10 símbolos); notificaciones cliente → `lib/client_notifications.py` (12 símbolos); generación de guías → `lib/shipping_guides.py` (2 símbolos, ~470 LOC con claim-before-bill Aveonline). Todo verbatim vía script AST con verificación byte-a-byte; namespaces del router intactos (0 cambios en callers); suites verdes en cada corte. El router queda cohesivo (webhook + procesamiento de evento + persistencia). **Monolitos restantes:** ninguno con corte trivial pendiente — `agentic/dispatcher.py` quedó en **3502** (gates determinísticos → `agentic/deterministic_gates.py`, 8 funciones verbatim; los lazy imports `from orchestrator import ...` viajaron con ellas, ciclo intacto) y su núcleo `_run_agentic_full` (2367 LOC en UNA función) **no es cortable por extracción** — requiere diseño propio (state handlers por estado FSM), sesión de arquitectura dedicada. **Ya cortados:** wompi_webhook 2664→1159, orchestrator 2632→1693, shipping_quote_tool 2019→1660, worker 4450→3609, dispatcher 4239→3502 — 2026-08-13 | P2 | Sin cambio de comportamiento; suite completa entre cortes |
| G13 | `config.py` por servicio con pydantic-settings (161 env vars) — **Fase 1 CERRADA 2026-08-13:** `services/api/config.py` — 30 vars críticas (Supabase, internal-secret, LLM, Resend, MeLi, MFA, gateway, rate-limit, Sentry, flag guías) declaradas con tipo+default, validadas agrupadas en boot vía `validate_critical()` (los 3 checks históricos + 5 nuevos de coherencia prod: secret corta, localhost en CORS, sin Sentry/Gemini, MeLi redirect http). `_validate_startup_config` de main.py delega ahí. 14 tests (`test_config_g13.py`). pydantic-settings 2.15.0 añadido a requirements del api. **Fase 2 (2026-08-13):** puntos que congelan al import migrados a `get_settings()` en el api (`internal_auth.py` secrets, `main.py` CORS — con el atributo de módulo preservado para los tests que lo parchean); config replicado a connector + orchestrator. **Criterio deliberado:** las flags runtime-fresh (MFA_*, body-cap por request, intervalos de jobs) NO se migran al cache — su lectura fresca por llamada es la que permite el flip operativo sin redeploy; quedan declaradas en config para boot. **Fase 3 EVALUADA Y CERRADA con alcance deliberado (2026-08-14):** los 74 puntos `os.getenv` restantes del api son de lectura única al arranque o runtime-fresh por diseño (flip sin redeploy) — migrarlos al cache no cambia comportamiento y suma riesgo sin beneficio real. El valor de G13 (fuente única + validación en boot + puntos críticos de dinero/seguridad centralizados) ya se capturó en fases 1+2. La migración del resto queda SOLO si un punto duele de verdad (drift/bug de config) | P2 | Migrar primero vars de dinero/seguridad |
| G14 | ~~`webhook_framework` ABC: adoptar-o-borrar~~ → **CERRADO 2026-08-13 (borrado parcial verificado):** la ABC `WebhookHandler` + `signature.py` + `idempotency.py` tenían 0 adopción → retirados; `rate_limit.py` + `errors.py` SÍ tienen consumo productivo (`integration_client/base.py`) → conservados; `__init__.py` reescrito sin refs a Envia; tests migrados a `test_webhook_rate_limit.py` (72 verdes) | P2 | Si Shopify (fase 13) u otro webhook llega, se reconstruye la abstracción con el conocimiento ganado |
| G19 | Embeddings con task-prefixes (calidad RAG) | P2 | Requiere ventana de re-embed (`scripts/admin/reembed_kb_documents.py`) + `embedding_model_version` nuevo |
| G20 | Cuota Gemini: separar keys por proyecto + sacar la key del web (3 route handlers la ponen en query string: `insights/route.ts:346`, `ai/preview/route.ts:14`, `ai/index-pending/route.ts:24`) | P2 | Founder crea proyectos/keys en AI Studio; agente migra los 3 handlers al patrón proxy del api (drift D3) |
| G24 | Verificar Python real en Render + migrar pin a `.python-version` 3.13 | P2 | **Compatibilidad VERIFICADA 2026-08-13:** suite completa (4352 passed / 0 failed) bajo Python 3.13.15 standalone aislado — código + deps de los 3 servicios OK en 3.13. **Queda solo el flip del founder en Render** (runtime.txt → 3.13 o `.python-version`), cambio de deploy con rollback fácil |
| G25 | Realtime: canales públicos `postgres_changes` → Broadcast privado con policies | P2 | Deuda de escalado (trigger: multi-tenant >2); documentado |
| G27 | MeLi `missed_feeds` (recuperación de notificaciones perdidas) | P2 | Trigger: primer tenant MeLi conectado (mismo que ritual IPs §D); hoy 0 tenants → sin riesgo activo |

---

## C. Roadmap post-go-live

| Iniciativa | Trigger / gate | Estado |
|---|---|---|
| **Konvi Studio** (Camino D — editor de personalización, react-konva, ~6-8 sem de dev) | **Gate comercial duro**: Lucams valida demanda con flow manual (Instagram + WhatsApp + Wompi link directo + diseño a mano) hasta **>30 órdenes/mes**. NO arrancar antes (contexto en `.context/04-next-steps.md` §"Konvi Studio") | Gated, sin código |
| **COD / contraentrega (H.2.4)** | Pausado formalmente 2026-05-07. Trigger de reanudación: KAIU completa KYC Ecart Pay Colombia + Prueba 3 en producción + ejecutivo confirma formato DANE Servientrega (V.4) + Coordinadora. Certificado: 4 carriers COD viables; no existe webhook COD dedicado (evidencia en `.context/04-next-steps.md` §"COD H.2.4") | Pausado formal |
| **Shopify / tienda custom (Fase 13)** | Futuro lejano; `services/connector-shopify` es placeholder | No iniciada |
| **Platform Console (Fase 12)** | Bloqueada por **OQ-P01** (¿misma app o app separada?) sin resolver — [`risks/open-questions.md`](risks/open-questions.md). Todas las vistas cross-tenant se difieren aquí | Bloqueada |

---

## D. Rituales operativos

| Ritual | Frecuencia | Próxima ejecución | Qué se verifica |
|---|---|---|---|
| Revisión de IPs de notificaciones MeLi | Trimestral o al conectar el primer tenant MeLi (lo que ocurra primero) | Verificada 2026-08-03 sin tráfico (0 tenants MeLi); próxima: al primer tenant conectado | Diff IPs oficiales vs `_MELI_DEFAULT_NOTIFICATION_IPS` en `meli_webhook.py`; alerta temprana: log `meli_webhook.alert_threshold_exceeded` |
| Re-certificación E2E conversacional | Cada release que toque bot (FSM, tools, invariants, prompt) | Con el próximo cambio de bot | `coherence_scenarios.py` 15/15 + `test_a11_coherence_assertions.py` verde; log en `scripts/uat/runs/` |
| Rotación de secretos | Trimestral y tras cualquier incidente | Tras cerrar B2 (primera rotación documentada) | Supabase keys, DB password, Meta App Secret, Wompi keys, `INTERNAL_SERVICE_SECRET`, `RESEND_API_KEY` |
| Revisión de este PLAN | Semanal | Próxima sesión de trabajo | Checklist §A al día, P0/P1 re-priorizados, estados verificados contra código (no contra memoria) |

---

## E. Bitácora de ejecución

Registro de hitos del cierre pre-producción. Cada cierre de ítem del checklist §A o del backlog §B debe reflejarse aquí y en su fila correspondiente, en el mismo commit.

| Fecha | Hito | Evidencia |
|---|---|---|
| 2026-08-02 | Auditoría profunda 8 dominios (0% suposición) + depuración documental + 13 docs canónicos + hardening código + pack UX wow + certificación local | `.audit/findings/2026-08-02-consolidated-audit.md`; gate `--ci` 24 OK / 0 ERROR, coverage 69.5% |
| 2026-08-02 | Migración `20260802120000` aplicada a prod (ghost tables droppeadas, grants de infra revocados) | Ledger prod = 251 = repo; `supabase db query --linked` |
| 2026-08-03 | 5 commits temáticos + push a `develop` (`1d05ce28`…`422d2336`) | `git log origin/develop` |
| 2026-08-03 | CI #30777152451 falló en job db-harness: baseline `tests/dbharness/schema_baseline.sql` no regenerado tras la migración (gate local `--ci` no incluye ese job — lección registrada). Fix: baseline regenerado con replay real (`scripts/schema_drift_check.sh --update`, podman) + check verde | Run fallido + commit `09e5f80d` |
| 2026-08-03 | CI #30778256256 falló de nuevo: `test_solo_service_role_accede_inbox` asumía SELECT permitido a `authenticated` (solo RLS lo frenaba); tras el REVOKE la barrera es anterior (InsufficientPrivilege). Fix: test acepta ambas posturas. Verificado local: **201/201 dbharness passed** con stack podman | Commit `112689ba` |
| 2026-08-03 | **CI VERDE en develop** (#30778777844): los 5 jobs success (validate.sh, py-core, db-harness, Next.js build, detect changes) | `gh run view 30778777844` |
| 2026-08-03 | Segregación de ambientes decidida por founder: ítem §A #16 (reactivar dev cloud al lanzar) + §B ENV-1 (sandbox local podman como DEV por defecto) | Este PLAN; `docs/infra/environments.md` §0 |
| 2026-08-03 | **Deploy a producción** (`git push origin develop:production`, `5fdad396..bba65b03`). Verificación funcional post-deploy: konvi-api `/health` reporta `version: bba65b03…` (SHA exacto), orchestrator worker reiniciado (heartbeat 2.5s), connector ok, web 307→/dashboard. En prod quedan activos: guardrails dinero fail-closed, deadline cascada 100s, polling Aveonline, 12 endpoints rate-limited, pack UX wow. Sin activar: guías reales (B1) ni MFA (A1) | Healthchecks live 2026-08-03 ~05:50 |
| 2026-08-03 | Purga anti-ruido documental (decisión founder): HANDOFF/environments/agentic reescritos sin arqueología; 5 docs muertos archivados; 12 canónicos corregidos por hechos stale; criterio vigente: la doc dice lo que ES y lo que FALTA | Commit `bba65b03`; corpus 123 vigentes / 81 archivados |
| 2026-08-03 | **ENV-1 CERRADO:** dev local por defecto contra Supabase local (podman). `.env` + `apps/web/.env.local` → `127.0.0.1:54321`; stack completo `supabase start`; bootstrap sandbox OK (tenant `d0000000-…-0001` + 2 owners); env_guard `dev-safe`; smoke REST/py/web/API verdes. Respaldos `.pre-env1` gitignored para rollback | `docs/infra/environments.md` §2.1; `tests/test_env_guard.py` (34 passed); `services/api/main.py` (http loopback) |
| 2026-08-03 | **"BLOQUE K/L" CERRADO (parte local):** harness `coherence_scenarios.py` sincronizado con el gate de método de pago y corrido contra el stack local (connector+api+orchestrator+Supabase podman, tenant sandbox) → **15/15 escenarios verdes** (5m 25s, gemini-3.1-flash-lite). Cambios: turnos que responden el gate contraentrega/online + assertion `check_asks_payment_method`, acciones de harness `!ensure_shipping`/`!reset`, reset determinista por escenario, seed catálogo UAT. Hallazgo: bug real `payment_coherence` CASE B2 corrompe la pregunta de modo de pago (repro aislada + 3 observaciones live) — reportado en el log, no corregido aquí. Parte live de B4 sigue founder-gate | `scripts/uat/runs/coherence_local_2026-08-03.md`; `tests/test_a11_coherence_assertions.py` 14 passed |
| 2026-08-03 | **UAT guía REAL Aveonline (B1) VERDE:** fix del endpoint `guide-dry-run` (select jsonb `shipping_origin` + idagente desde credenciales + dual-auth + gate MFA internal-aware en router integrations) → guía real **86732771636** (COORDINADORA, ruta local Bogotá→Bogotá, $7.530) generada vía API local con env prod y verificada `GENERADA` en Aveonline. Doble fail-safe confirmado; flag per-tenant revertido. Hallazgo en vivo: anulación de guía individual NO existe vía API (dossier §8.2 confirmado: `cancelarGuia` "parametro incorrecto", v2 404/401) → anulación manual panel pendiente sobre la guía UAT. Conclusión: flip **listo para aplicar** (render.yaml NO tocado — lo aplica el orquestador) | `scripts/uat/runs/aveonline_guia_real_2026-08-03.md`; `tests/test_aveonline_guide_dry_run.py` (5 passed); `services/api/routers/integrations.py`, `services/api/main.py` |
| 2026-08-04 | **B1 CERRADO — guías reales ACTIVAS en prod.** Doble compuerta aplicada y verificada vía Render API: `real_guides_enabled=true` (KAIU, `tenant_shipping_provider_config`) + `AVEONLINE_GENERATE_REAL_GUIDES=true` en konvi-api y konvi-orchestrator (commit `2a0c4f6c`, deploy `fe8bc539` verificado por SHA). Env vars del worker confirmadas en Render: `WOMPI_INBOX_RECONCILE_*` y `AVEONLINE_STATUS_POLL_*` sincronizadas (polling tracking activo). CI tuvo un fallo intermedio por osv-scanner (DB OSV fresca: rollup/brace-expansion/undici/fast-uri) → parcheado con overrides en `package.json` (`fe8bc539`, gate "No issues found"). Pendiente founder: anular guía UAT 86732771636 en panel Aveonline | Render API env-vars (2026-08-04); CI #30866337026 success |
| 2026-08-04 | **Cierres de clasificación del backlog** (sesión "continuar el plan"): §A #10 verificado en prod (0 filas `active_provider='envia'`); M4 (runbook Wompi ya existía, verificado); M20 (placeholders documentados); A7 (verificación final 01-state/AGENTS con grep, 0 menciones stale); M16 (espejos Aveonline idénticos + test de paridad en CI → controlado, unificación diferida); M7 (diferido: esfuerzo L, 3 barreras de aislamiento verificadas); M19 (bloqueado por B2: rotar verify_token exige consola Meta del tenant dev); F8 (fuera del cierre: feature nueva); "B4-infra" (obsoleto: 4 servicios en plan Starter always-on, verificado Render API) | Queries prod + Render API + greps; commits `1aa55aee`, `0b537f0e` |
| 2026-08-07 | **M6 CERRADO — rama `audit_log` en `fn_apply_retention`:** migración `20260807120000` aplicada a prod (ledger 252), dormida (política sigue `enabled=false`; guarda estructural en 2 niveles). Probada local (5 casos SQL) y en prod (79 filas intactas); dbharness 201 verde + baseline regenerado | Commit `bcc9dfa3` |
| 2026-08-07 | **M17 CERRADO — refresh proactivo tokens MeLi:** endpoint interno `POST /api/v1/internal/meli/refresh-tokens` (dual-auth sin X-Tenant-Id) + job `meli_token_refresh` en el worker (6h; el refresh_token rota ≥4 veces/día sin actividad, lejos del TTL ~6 meses). 12 tests nuevos. Parte Telegram (setWebhook manual): cierre documental (runbook en `docs/integrations/telegram.md`) | Commit `edd8b0da` |
| 2026-08-07 | **A2/T4-01 CERRADO — `cf-connecting-ip` NO spoofeable:** Cloudflare (edge de Render) rechaza con 403 (error 1000) todo request que traiga el header seteado por el cliente — nunca llega a la app. Evidencia: log `[XFF_CANARY]` request normal (`resolved`=IP real) vs 4 requests spoofeados muertos en el edge. Allowlist MeLi y rate-limits por IP sobre valor no controlable. Canario apagado y verificado. **Colateral: bug real webhook MeLi 401** — el endpoint usaba `Depends(get_service_client)` (exige JWT) y respondía 401 a llamadas sin JWT (MeLi no trae JWT; 0 tenants hoy, sin impacto activo). Fix alineado a los otros 3 webhooks + test de wiring anti-regresión (`WebhookPublicoSinJWTTests`) + purga polución `sys.modules['integrations*']` en `test_aveonline_guide_dry_run.py` (fallo CI #31202076206). 81 tests MeLi verdes, ruff sin regresión | Commit `1f1b5ab9`; script `scratch/t4_01_xff_canary.py` |
| 2026-08-07 | **M9 EN CURSO — portabilidad de tests (SIN commitear):** transformación aplicada en el working tree (255 archivos .py: 240 en `tests/`, 15 en `scripts/` — paths absolutos `/home/ansible` → `Path(__file__).resolve().parents[N]`); shims symlink eliminados de `.github/workflows/ci.yml`; portabilidad probada en `/tmp` (4.152 coleccionan sin errores); polución cross-test corregida (668 passed en el combo que fallaba). **PENDIENTE en la próxima sesión:** correr `python3.11 -m pytest tests/ -q -m 'not dbharness'` completa verde → commitear → verificar CI → eliminar esta fila y marcar M9 ✅ | Working tree local; agente perdido a mitad de verificación final |
| 2026-08-13 | **M9 — verificación final VERDE (pendiente solo commit + CI):** suite completa sobre el working tree portable → **4143 passed / 23 skipped / 201 dbharness deselected** en 264s; **0 paths absolutos restantes** (grep en `tests/` + `scripts/`); shims symlink eliminados de `ci.yml`. Listo para commit (requiere autorización founder) → CI → marcar M9 ✅. Misma sesión: frescura del PLAN verificada contra código — A1 sigue `false` (`render.yaml:208-209`, founder-gate), `COVERAGE_MIN=60` vigente, fix del bug CASE B2 constatado en `d01b2fd2` (fila §A #4 actualizada) | `pytest tests/ -q -m 'not dbharness'` 2026-08-13 |
| 2026-08-13 | **M9 CERRADO:** commits `850a2849` (portabilidad 255 archivos) + `298ad1a2` (docs) pusheados; primer run CI `31731556822` falló por advisories JS publicados ese mismo día (js-yaml 4.3.0 GHSA-5p4m, nanoid 3.3.16 GHSA-2v37 — ambos CVSS alto con fix patch) → fix `996b5e95` (overrides pnpm, patrón de `fe8bc539`). **CI run `31732756174` VERDE — 5/5 jobs** (validate.sh full, py-core, db-harness, Next build, detect changes); la suite corrió en CI sin shims symlink por primera vez | `gh run view 31732756174`; gate osv local "No issues found" |
| 2026-08-13 | **Gaps G1-G28 ejecutados (lotes A-G, 7 agentes + integración):** 14 cerrados + 2 parciales + 12 agendados (detalle y evidencia en §B.2; origen `.audit/findings/gap-analysis-2026-08-13.md`). Verificación integrada: suite **4133 passed / 23 skipped / 201 dbharness deselected** (exit 0, 261s) + tsc 0 errores + Vitest 320 + tenant-lint 0 gaps + ruff 201 ≤ baseline 202. Fixes de integración del orquestador: colisión namespace `services` (orchestrator vs connector) en 2 tests nuevos → patrón importlib con restauración; fallback "Envia" en shipping page → neutro; typo `pnmp@` en render.yaml; comentarios CI/conftest/manifest obsoletos tras G28 | Esta sesión; log `.audit/logs/pytest-integration-final.log` |
| 2026-08-13 | **G5 CSP nonce CERRADO** (lib/csp.ts + proxy + layout; verificado live: 27/27 scripts con nonce en /login, header↔body coincide; 16/16 tests; CI `aae1cc78` verde) y **G9 fase 1 CERRADA** (worker en 2 loops asyncio `_INBOUND_JOBS`/`_MAINTENANCE_JOBS` + `inbound_lag_seconds`; `_poll_cycle` preservado; 10 tests nuevos; 3 tests ajustados al nuevo registro por tuplas; suite 4142 verde; CI `2a97975b` verde) | commits `aae1cc78`, `2a97975b`; `.audit/logs/pytest-g9-final.log` |
| 2026-08-13 | **M18 CERRADO — coverage 72.7% (gate 60→70):** 207 tests nuevos vía 3 agentes (conversations 36→100%, marketplace 28→100%, integrations 43→99%). Suite 4349 passed / 0 failed. Fix aislamiento: excepciones resueltas en ejecución (colisión paquete `integrations` api↔orchestrator, patrón del repo). **Observaciones sin bug reportadas por los agentes:** `update_conversation_status` llama `abort_idempotency` dos veces en path 404 (redundante, inofensivo); `_import_meli_item` reporta `compare_at_price` crudo en el resumen aunque no lo persista si ≤ price (cosmético) | `.audit/logs/coverage-m18-final.log` (TOTAL 72.7%, 4349 passed) |
| 2026-08-13 | **Ronda 2 de gaps:** G3 COMPLETO (Idempotency-Key en payment-link: replay + finalize/abort, patrón create_order; 6 tests ajustados + 3 nuevos) · G14 CERRADO (webhook_framework: ABC/signature/idempotency sin adopción retirados; rate_limit/errors — que SÍ consume integration_client — conservados; tests migrados, 72 verdes) · G20 CERRADO (key Gemini FUERA del web: nuevo router `insights.py` en api con puerto íntegro + `ai/preview`/`ai/index-pending` proxificados al api; `GEMINI_API_KEY`/`GEMINI_MODEL` fuera de konvi-web en render.yaml; 25 pytest + 346 vitest + tsc verdes) · Observaciones M18 corregidas (abort duplicado en conversations unificado; resumen import MeLi reporta `compare_at_price` persistido). Verificación integrada: suite **4338 passed / 0 failed** + ruff 202 (= baseline) + tenant-lint 0 gaps + tsc 0 + vitest 346 | `.audit/logs/pytest-ronda2.log` |
| 2026-08-13 | **Preparación ejecutable de founder-gates (agente, autonomía founder):** rotación sin-caída de `INTERNAL_SERVICE_SECRET` implementada — dual `INTERNAL_SERVICE_SECRET_PREVIOUS` en api (`internal_auth.py`) y orchestrator (`server.py`); 10 tests nuevos (`test_internal_secret_rotation.py`), 18/18 verde con regresiones A11. Runbooks ejecutables: [`operations/runbooks/credential-rotation.md`](operations/runbooks/credential-rotation.md) (B2: matriz sin-caída/ventana verificada contra docs oficiales Supabase/Meta/Wompi/Resend/Gemini + `render.yaml`; orden, verificación, rollback; M19 = paso 11) y [`operations/runbooks/mfa-mandatory-rollout.md`](operations/runbooks/mfa-mandatory-rollout.md) (A1: día 0/X, verificación, rollback) | Esta sesión; tests + gate local |
| 2026-08-14 | **Reorganización de ambientes y `.env` (decisión founder: real PRD, sin basura):** 2 ambientes definidos (PRD=Render+Supabase cloud · STG=Local+Supabase OSS podman). Contrato canónico `.env.example` línea a línea por proveedor con tags [PRD]/[STG]/[AMBOS]/[SCRIPTS]/[ELIMINAR] + **guard anti-drift `tests/test_env_contract_guard.py` en CI** (cobertura código→contrato, sin-basura contrato→código, consistencia muertas). Consolidación a 2 archivos reales: `.env.local` (raíz, servicios Python) + `apps/web/.env.local` (propio del web — paridad konvi-web; el symlink que intenté se REVIRTIÓ por romper la paridad) + `.env.prd-backup` (backup de plataformas). **9 vars muertas eliminadas** de todos los archivos (META_APP_SECRET/VERIFY_TOKEN, CUSTOMER_CONTEXT, USE_NEW_ORCHESTRATOR, TELEGRAM_BOT/CHAT global, CART_RECOVERY — verificado 0 usos) + `.pre-env1`/`.bak-*` fuera (contenían las keys comprometidas de B2). Flujo git limpio: solo `.env.example` trackeado. Suite 4386 verde | `.env.example`; `tests/test_env_contract_guard.py`; suite `.audit/logs/pytest-env.log` |
| 2026-08-14 | **Diseño canónico de segregación total de ambientes (0 suposición, verificado contra código):** nuevo doc `docs/infra/environment-segregation.md` — integración por integración (Wompi, Meta, Aveonline, MeLi, Telegram, Resend, Gemini, Supabase): qué cuenta/llaves/URL de webhook usa cada ambiente y cómo decide el código (per-tenant vía Vault vs env var por deploy unit). URLs de webhook externas verificadas en routers (5 paths). **5 gaps reales mapeados:** dominio propio (OQ-4), dev cloud (#16), guard anti-mezcla de tenants (ejecutable por agente), MeLi app de prueba (founder), setWebhook Telegram por ambiente (HITL documentado) | `docs/infra/environment-segregation.md`; enlazado desde `docs/infra/environments.md` |
| 2026-08-15 | **Gap #3 segregación CERRADO — guard anti-mezcla tenant-data ↔ ambiente:** `scripts/check_env_data_mix.py` (complemento data-level de `_env_guard.py`, que solo clasifica destino). Reglas: STG + Wompi `environment='production'` (cualquier status — las llaves ya estarían en el Vault sintético) → FAIL; STG + `real_guides_enabled=true` → FAIL; PRD + Wompi connected en sandbox → WARN (`--strict` la vuelve falla). Meta/Telegram documentados como no auto-chequeables (Vault + dashboard proveedor). **Certificado live:** 13 tests verdes; STG real limpio exit 0 → violación sintética sembrada (`real_guides_enabled=true` en tenant sandbox) → FAIL exit 1 → limpiada → exit 0; PRD read-only exit 0 con el WARN esperado (KAIU sandbox — pendiente founder Wompi prod keys). Colateral: stack local estaba caído (container DB exited) → reiniciado; sandbox ENV-1 reconstruido con `bootstrap_dev_sandbox.py` (auth.users pendientes de re-enlazar, warning del script) | `scripts/check_env_data_mix.py`; `tests/test_check_env_data_mix.py`; corridas live STG/PRD 2026-08-15 |
| 2026-08-15 | **CI destapó falla latente de `validate.sh` §7 (enmascarada por runs docs-only):** el push del guard (`af0408fa`) falló en validate.sh — no por el guard (suite 4412 verde, coverage 72.9%) sino porque §7 exigía 4 vars de tuning (`PENDING_PAYMENT_RELEASE_ENABLED`, `CONVERSATION_HISTORY_LIMIT`, `API_RATE_LIMIT_DISTRIBUTED`, `ANTI_HIBERNATION_ENABLED`) como líneas `KEY=` en `.env.example`, y el contrato canónico (`cce54233`) las documenta como comentario del tuning-block a propósito (defaults fail-safe en código; regla sin-basura). Los runs verdes intermedios eran docs-only → `detect changes` skipeaba validate.sh (lección: CI verde en docs-only NO certifica el pipeline). Fix: las 4 salen de `required_example` con nota — su presencia PRD ya la guarda §6 render.yaml (4/4 ✅) y `test_env_contract_guard.py` cubre documentación de toda var leída. **CI `00326fdb` 5/5 jobs success** (validate.sh full incluido) | Commit `00326fdb`; run `31916164404` |
| 2026-08-16 | **Plan de segregación TOTAL de ambientes (código + plataformas) — investigación documental profunda 6 frentes (~70 URLs oficiales, 0 suposición):** nuevo doc `docs/infra/environment-segregation-plan.md` con matriz verificada por plataforma + plan por fases S0-S6 con owner y verificación por paso. **Hallazgos que cambian el diseño:** Wompi exige URL de eventos distinta por ambiente y envía `environment` en cada evento (defensa en profundidad nueva → S0.1); Meta: 2 apps sobre la misma WABA = webhooks duplicados → STG exige WABA/número propios (Test Apps oficiales); MeLi no tiene sandbox (test users en prod, máx 10, expiran 60d) y sus webhooks solo se defienden por 4 IPs (allowlist vigente en código, sin cambio detectado en snapshots — re-verificación contra doc viva bloqueada por devsite HTTP 500); **Aveonline SÍ documenta hoy un "API Sandbox"** (empresas demo 6077/25505 — el dossier local 2026-05-21 quedó desactualizado) y su webhook es único por empresa; Resend/Gemini sin test mode/sandbox (segregación = key `sending_access` / proyecto GCP separado; free tier Gemini entrena con datos → prohibido en PRD); Supabase: legacy anon/service_role **ya no rotan** — pasos exactos de revocación mapeados para B2 paso 10; Render: Projects/Environments + env groups con scope + custom domain conservando onrender.com para transición de webhooks sin corte | `docs/infra/environment-segregation-plan.md`; investigación 6 agentes verificada contra docs oficiales |

| 2026-08-16 | **Fase S0 del plan de segregación CERRADA (4/4) — endurecimiento anti-mezcla Wompi en código:** (S0.1) el webhook ahora rechaza eventos FIRMADOS cuyo `environment` ("test"/"prod") no corresponde al `meta.environment` del tenant — defensa en profundidad sobre la firma SHA256 (corta el caso "events_key de un ambiente + meta del otro"); helper puro `payload_environment_matches` en `wompi_client.py`, inserción en `_process_wompi_event` paso 3.6 (cubre path durable). 11 tests nuevos (helper + router: cross-env rechazado con firma válida, match procesa, sin-campo backward compatible) — `test_wompi_webhook.py` 24/24 verde. (S0.2) el guardado de credenciales (server action `saveWompi`, único path de escritura — la UI de setup es read-only) valida prefijos de llave vs ambiente (`prv_test_`/`test_events_` vs `prv_prod_`/`prod_events_`) vía lib pura `apps/web/lib/wompi-keys.ts` — 7 Vitest + tsc verdes. (S0.3) dossier Aveonline §12 con addendum 2026-08-16 (API Sandbox empresas demo 6077/25505 + webhook único por empresa). (S0.4) MeLi: IPs del allowlist sin cambio vs snapshots oficiales; doc viva bloqueada por devsite HTTP 500 (pendiente de tercero, registrado). Ruff 201 = baseline (0 regresión) | Commits de esta sesión; tests + ruff local |

| 2026-08-16 | **Fase S7 CERRADA — STG 100% local homologado total a PRD (decisión founder: STG 0 dependencia de Render):** filtro de env por servicio `scripts/dev_env_for_service.py` (set de vars = render.yaml por servicio; precedencia: local gana · anclas de ambiente nunca heredan PRD — `AVEONLINE_GENERATE_REAL_GUIDES` incluida, evita compuerta de guías reales abierta en STG · tuning con `value:` heredado = idéntico a PRD · sync:false solo con delta documentado). Makefile homologado (orchestrator `uvicorn server:app :8002` = PRD, antes `main.py` sin /health; entorno limpio `env -i` por servicio — fin del megáfono) y **ahora trackeado en git** (`.local/` entero estaba gitignored — la orquestación local era invisible al repo). Guard CI `tests/test_stg_prd_parity.py` (13 tests: snapshot de conteos, precedencia, entrypoints, cobertura contrato). Certificación live `scripts/certify_stg.sh` = **18/18** (health ×5 en los paths de render.yaml, aislamiento probado en /proc de procesos vivos, internal-secret orchestrator 200 + api dual-auth 422, web→api). **Bugs reales destapados y corregidos:** drift Sentry PRD (client leía `NEXT_PUBLIC_SENTRY_TRACES_RATE`, render.yaml setea `..._SAMPLE_RATE` → override nunca aplicaba; unificado al canónico F97), quoting de valores con espacios en env filtrado, contrato `.env.example` completado (PLAN_ENFORCEMENT_ENABLED, API_RATE_LIMIT_ENABLED/DISTRIBUTED, MULTIMODAL_AUDIO_ENABLED, build-vars web). Suite 4423 passed / 0 failed + vitest 353 + tsc 0 + ruff = baseline | `scripts/certify_stg.sh` 18/18 live; suite `.audit` log; commit de esta sesión |

| 2026-08-16 | **CI S7 5/5 verde (run 31923901995, commit `6abf629a`) + plan actualizado con decisiones founder:** nueva **FASE S8 — eliminación TOTAL de Sentry** (decisión founder: free tier vencido, no útil; observabilidad propia se construirá en la fase Platform Console) con inventario verificado contra código (43 archivos con menciones: sentry-sdk ×3 requirements + observability.py ×3 + 13 call sites Python, @sentry/nextjs + 3 configs + wrapper next.config + 7 archivos web, paquete `packages/observability` MUERTO — nadie lo importa, 19 keys en render.yaml, sección del contrato, tests con asserts Sentry) y criterio de cierre grep-cero + certificación completa. Respuestas founder incorporadas al plan (§3.5): comunicación de endpoints en local = túneles ngrok (solo para webhooks de terceros; tráfico interno directo localhost; verificado que las credenciales de túnel NO llegan al runtime de servicios) y dominio API: **onrender.com es transitorio, NO el estado objetivo** (dominio propio fase S3; la conservación del subdominio onrender permite migrar webhooks sin corte) | Run CI 31923901995; `docs/infra/environment-segregation-plan.md` §S8/§3.5 |

| 2026-08-17 | **Fase S8 CERRADA — eliminación TOTAL de Sentry (agente):** sentry-sdk fuera de los 3 requirements; `observability.py` ×3 borrados (el del orchestrator albergaba el OTEL rev.109 → movido intacto a `tracing.py`, NO era Sentry); 13 call sites → `logger.exception`/logs existentes (`_surface_email_failure` eliminado — cada caller ya logueaba; sus 4 tests ahora asertan la clasificación vía assertLogs); `@sentry/nextjs` + 3 sentry configs + `instrumentation.ts` + `withSentryConfig` fuera del web (call sites → console.error); `packages/observability` borrado (muerto, nadie lo importaba); 17 keys SENTRY_* fuera de render.yaml + sección del contrato `.env.example`; `_ENV_ANCHORED`/`_STG_DELTA_OK` sin SENTRY_*; snapshot de paridad actualizado (22/7/38/99 → 13/5/35/96); guard M16 de espejos observability removido con los módulos; `test_w1_sentry_pii_scrub.py` → `test_w1_phone_masking.py` (solo masking en origen, vigente); `_reorg_env_f2.py` borrado (one-off muerto); falso positivo grep `PiiAccessEntry` renombrado en audit page. Docs vivos a la postura sin-Sentry (TRD/BACKEND/slo-and-dr/local-prod-symmetry/wompi/pago-wompi/.context ×3/runbooks/legales ×2) + `sentry-setup.md` archivado. **Certificación local: grep-cero en código/config · suite 4408 passed/0 failed · vitest 353 · tsc 0 · `next build` OK · ruff 197 ≤ baseline 201 · `certify_stg.sh` 18/18 live** (contenedores podman venían exited de un reinicio de VM — `supabase stop/start` limpio y re-certificado). **Pendiente [F]:** borrar env vars SENTRY_* en el dashboard Render ×4 servicios + cancelar el proyecto en sentry.io | Suite log + `scripts/certify_stg.sh` 18/18 (2026-08-17); commit de esta sesión |

| 2026-08-17 | **CI S8 5/5 verde (run 32086032295, commit `581bcd40`)** — validate.sh full, py-core, db-harness, Next build, detect changes todos success tras el borrado total de Sentry. S8 queda cerrada en la parte agente; único remanente: paso [F] dashboard (borrar SENTRY_* en Render ×4 + cancelar proyecto sentry.io) | `gh run view 32086032295` |

| 2026-08-19 | **S2 CERRADA — Meta WhatsApp E2E live en STG:** Test App `KAIU Chat - Test` (912826941411258) + WABA prueba 2159052118202272 + número prueba 990364080831295 cableados al connector STG vía ngrok. Fallos encontrados y corregidos en vivo: webhook sin `{tenant_id}` en el path (404 — `print-urls` corregido para imprimirlo), token con typo de un carácter (rotado a uno generado + sembrado), app no suscrita a la WABA (`POST /subscribed_apps` — 0 mensajes llegaban). **E2E verificado: WhatsApp real → POST Meta 200 (HMAC Vault OK) → inbound procesado → bot respondió → Meta aceptó outbound (wamid) → respuesta recibida en el teléfono del founder (confirmado por él 2026-08-19: saludo + menú de categorías del catálogo sembrado).** Sandbox STG completado: 2 owners enlazados (auth.users recreados tras reinicio VM) + catálogo 7 productos. Registrado en `environments.md` §2. Pendientes menores: System User token permanente (el actual expira ~24h) + desuscribir apps prod de la WABA de prueba [F] | Log connector/orchestrator live + queries DB local 2026-08-19 |

| 2026-08-20 | **S5 CERRADA (3/3 E2E en STG) + auditoría Makefile local:** (a) **Resend** — key `konvi-stg` (sending_access) con envío real a `delivered@resend.dev` por el path del proyecto; política `RESEND_FROM_EMAIL` por ambiente definida (STG jamás desde `konvi.co` a direcciones sintéticas — bounces dañan reputación [DOC Resend]; objetivo: subdominio `stg.konvi.co` con S3). (b) **Telegram** — bot `@konvi_stg_bot` + grupo `Konvi STG Operadores` (chat_id `-5381900925`); notificación de escalación real entregada al grupo por `notify_escalation_async`; procedimiento canónico documentado en `docs/integrations/telegram.md` (chat_id de grupo SIEMPRE negativo; privacy mode exige `/start@bot` en el grupo; token per-tenant en Vault — corregida mi propia instrucción errada de env var). (c) **Gemini** — proyecto GCP `konvi-stg` separado (cuota/billing son por proyecto [DOC]) con auth key restringida; llamada real `gemini-3.1-flash-lite` OK. Colateral: **Makefile local auditado target por target** — nuevo `make db` (auto-recupera contenedor postgres exited tras suspensión de VM), preflight de DB en `up`, línea db en `status`; restart/down/clean/logs verificados sin huérfanos. `certify_stg.sh` 18/18 final. **Con S2+S5 cerradas, la FASE A (STG total con terceros reales) queda completa** | Envíos/llamadas live + `certify_stg.sh` 18/18 (2026-08-20); commit del Makefile |

| 2026-08-21 | **Batch de higiene STG tras observaciones founder (envs, arranque, secretos):** raíz de la "DB caída" encontrada — los contenedores rootless de podman NO auto-inician al boot de la VM → `make up`/`restart` ahora **auto-recupera la DB** (detecta, corre la recuperación, aborta si no logra) y se habilitó `loginctl enable-linger` (cura de raíz: contenedores auto-inician al boot). Web local forzado a **Node 22** vía nvm (fin del drift v20 vs `.nvmrc`/CI/Render; huérfano next-server v16.1.7 eliminado). Depuración de env files con política escrita en `environments.md` §3: `.env.prd-backup` sin `localhost`/ngrok (APP_URL/API_URL/PUBLIC_WEBHOOK_URL → valores reales prod), `.env.local` sin duplicados de tuning (`META_MEDIA_*` se heredan de render.yaml) y sin credenciales Meta en claro (ya en Vault — el token `SYSTEM:` resultó ser el **System User token permanente** de la Test App: sembrado reemplazando el temporal 24h). **`INTERNAL_SERVICE_SECRET` de STG rotado** — era IGUAL al de PRD (riesgo real: el secreto service-to-service de prod deducible desde STG); ahora distintos y verificado con `certify_stg.sh` 18/18 (wiring orchestrator→api OK con el secreto nuevo). `.env.example`: timeout META_MEDIA corregido a 10 (= código y PRD) | `certify_stg.sh` 18/18 + guards (2026-08-21); verificación `loginctl show-user` (Linger=yes) |

| 2026-08-21 | **Fix GUI local + DEPLOY A PRODUCCIÓN (`fe8bc539..1e9d54c0`, 97 commits certificados):** reproducción Playwright/Chromium del "Sin tiempo real" — dos bugs reales: Next 16 bloqueaba el dev server por LAN (anti-DNS-rebinding → página sin hidratar; fix `allowedDevOrigins`) y el browser apuntaba el realtime a 127.0.0.1 desde otros equipos (fix `resolveSupabaseUrl` + 5 tests; limitación residual documentada: el realtime local rechaza join con Origin de navegador por LAN — workaround SSH forward); indicador "Live" estático eliminado (señal falsa vs badge real). **Interlock migrate-before-deploy respetado:** la migración `20260814130000` (bucket privado inbox, G8b) estaba en el repo pero NO en el ledger prod → aplicada por protocolo (`db query --linked` + verificación del bucket + `migration repair`). Deploy verificado: los 4 servicios **live en `1e9d54c0`** (Render API) + health 200 ×5 (incl. `app.konvi.co/login`). PRD ahora corre: sin Sentry (código + envs), sin fallbacks legacy Supabase, con el hardening S0 de Wompi (environment cross-check) | Render API deploys + healthchecks live + `migration list --linked` (2026-08-21) |

| 2026-08-19 | **S8 [F] CERRADO + B2 paso 10 CERRADO (el P0 muere):** founder borró las env vars SENTRY_* de los 4 servicios Render (redeploys OK) y eliminó la organización en sentry.io → **verificado vía Render API: 0 vars SENTRY_* en konvi-web/api/orchestrator/connector** + health live 200 ×5 (`/health` ×3 backends, `/health/ready` api, `/login` web). Y lo más importante: founder **desactivó las legacy JWT-based API keys** (anon/service_role) en Supabase → `python3.11 .audit/check_old_creds.py` → **401 en ambas** ("Legacy API keys are disabled", fechado 2026-08-19 por Supabase) — las credenciales filtradas en `be739a4` (abril) ya NO abren la DB productiva. **Efecto colateral positivo: G23 (retiro de fallbacks legacy en código) queda desbloqueado para el agente.** Pendiente para cerrar B2 completo: solo M19 (el reset del DB password lo hizo el founder este mismo día — `.env.prd-backup` actualizado, runtime no la usa, CLI local verificado operativo) | Render API env-vars ×4 + healthchecks live + `check_old_creds.py` 401×2 (2026-08-19) |

| 2026-08-19 | **G23 CERRADO (agente) — retiro total de fallbacks legacy Supabase `anon`/`service_role`:** desbloqueado por B2 paso 10 (keys legacy desactivadas a nivel Supabase). Código: 15 puntos en los 4 servicios (auth.py, worker.py ×2, server.py, db_persistence.py, config.py ×3 — campos y validaciones) + 5 en web (admin/client/server/proxy/confirm route — solo `PUBLISHABLE`/`SECRET_KEY`) + ~35 scripts operativos/UAT/debug al canónico `SUPABASE_SECRET_KEY`. Config: 5 entries legacy fuera de render.yaml, `.env.example` sin las 2 vars legacy, validate.sh §7/§8 al canónico. Tests: fixtures renombrados en masa (~110 archivos), 3 tests del comportamiento fallback eliminados, snapshot de paridad actualizado (13/5/35/96 → 11/4/34/95). Precondición verificada vía Render API: `SUPABASE_SECRET_KEY` en los 4 servicios. **Certificación: suite 4405 passed/0 failed · vitest 353 · tsc 0 · next build OK · ruff 197 ≤ baseline · certify_stg 18/18 (procesos vivos con 0 vars legacy)**. Colateral: label stale corregido en `check_old_creds.py` (decía "actual prod: rjtjx…" hardcodeado — el prod real verificado vía Render API es `xmelwnhhphksbpdjmbbp`). **Único remanente [F]:** borrar las 5 vars legacy del dashboard Render (inertes — las keys ya están desactivadas) | Suite + certify_stg 18/18 (2026-08-19); commit `bbada5f1` + **CI 5/5 verde (run 32307062564)** |

**Pendiente inmediato:** **§B P0/P1 + gaps §B.2 + S0/S7/S8 + G23 CERRADOS; P0 eliminado (legacy Supabase 401).** El trabajo restante está consolidado en **[`PLAN-CIERRE.md`](PLAN-CIERRE.md) — plan de cierre controlado por ambiente** (Track 1 dinero real: Wompi prod keys en KAIU, anulación guía UAT, B4 live, B6/B3 legal · Track 2 STG con terceros: S2 Meta Test App, S5 Resend/Telegram/Gemini, S6 MeLi, M19 · Track 3 infra PRD: S3 dominio `api.konvi.co` + Render Projects, pin Python 3.13, dev cloud #16, G8b · Track 4 operación: A1 MFA, plantillas Meta). Todo es [F] founder sobre dashboards externos salvo verificaciones/G8b/B4 que ejecuta el agente con autorización. Diseño de fondo (no certificable sin UAT live B4): `_run_agentic_full` state-handlers FSM.

| 2026-08-22 | **TRACK 9 CERRADO EN STG — hardening RLS/grants DB (C1 + A1-A9 + M1-M14 + bajos + causa raíz):** 4 migraciones (`20260822120000` cola takeover + idagente · `20260822120100` altos · `20260822120200` medios · `20260822120300` bajos+causa raíz). **Cada exploit verificado ANTES (fallaba) y DESPUÉS (pasa):** C1 — 5 ataques ejecutaban (cola pgmq global con PII cross-tenant + DoS de escalaciones; demo manual: `SET ROLE anon` pisó `credentials.idagente` de un tenant, '6135'→'9999', rollback) · A — 22 fallos pre-fix (8 RPCs admin ejecutables por authenticated: meli lease, aveonline jwt, tracking events, capability quotas, plan capabilities, reversion oracle; claims/cancellations mutables; payments con raw_webhook PII sin gate; security_events mutable; miembro `inactive` leyó secreto Vault + credenciales carrier + ai_agents — ejecución real) · M — 16 fallos pre-fix (operator activaba `real_guides_enabled`, editaba configs críticas/shipments/listings/tracking, leía notification_settings con secret refs, editaba mensajes/conversaciones/contacts, movía notas a otro tenant, outbound_idempotency ejecutable; retención no podía purgar pii_access_log — trigger incondicional) · bajos — 7 fallos pre-fix (JWT-rol stale autorizó INSERT en tenant_users; 12 policies con rol del claim; alerta reasignable a otro usuario; 4 SECDEF sin search_path; 4 overloads legacy; TRUNCATE/REFERENCES/TRIGGER en ~70 tablas; función nueva nacía con EXECUTE authenticated). **Causa raíz:** demostrado empíricamente (PG 17.6, DB limpia) que `ALTER DEFAULT PRIVILEGES` NO quita el built-in PUBLIC EXECUTE de funciones nuevas → cerrado con **event trigger `track9_revoke_public_on_new_function`** (REVOKE automático en cada CREATE FUNCTION de public) + default ACL sin grants de cliente + **guard CI** `scripts/check_secdef_grants.py` (validate.sh §4.7: falla si una migración nueva crea SECDEF sin REVOKE/search_path; exención auditable `-- track9:exempt:<fn> — razón`; verificado con migración trampa) + **barrido vivo** `test_track9_secdef_hygiene.py` (0 SECDEF ejecutable por PUBLIC/anon post-replay). **Ajustes verificados contra DB real:** "9 funciones sin search_path" → eran 4 SECDEF · A7 con vista `payments_safe` (payments owner-only; `orders/[id]/page.tsx` migra a la vista — sin raw_webhook para members) · M14-storage sin acción (tenant-media: 0 objetos, bucket público legítimo del catálogo) · **hallazgo entorno: el Postgres 17.6 del build local crashea (signal 11) al denegar EXECUTE de función** (reproducido con dummy `SELECT 1`; prod Supabase managed no comparte el build — 5 migraciones REVOKE ya viven allí sin incidentes) → tests de grants por catálogo (`has_function_privilege`), patrón ya vigente. **Flujos consola STG verificados tras cada tier** (JWT owner real: orders/claims/inbox/payments_safe/notification_settings 200 con datos). **Replay limpio 262 migraciones + baseline regenerado + STG reconstruido** (tras el reset: bootstrap + catálogo + 6 secretos Vault re-sembrados con descifrado verificado E2E vía `get_aveonline_credentials`; mi error de proceso — sobrescribí el dump de datos pre-restore — quedó cubierto por la reconstrucción completa; lección: nunca re-escribir el archivo de backup antes de verificar el replay). **Certificación: suite 4543 passed/0 failed + dbharness 306 (99 tests de ataque nuevos) + vitest 358 + tsc 0 + ruff 196 ≤ baseline + certify_stg 18/18**. Migraciones listas para PRD en el descongelamiento (PLAN-CIERRE §Orden paso 9) | Suite + dbharness + `certify_stg.sh` 18/18 (2026-08-22); commits de esta sesión |

| 2026-08-22 | **DESCONGELAMIENTO PRD EJECUTADO (pasos 9-10 del §Orden):** pre-paso B1 verificado (**0 duplicados** pending_payment en prod → dedup no-op) · **8 migraciones aplicadas por protocolo** (`db query --linked` + `migration repair`) con verificación por tier en prod: B-0 ×3 (índice anti doble-cobro + columnas) + idagente RPC + Track 9 ×4 (C1: 3 RPCs infra cerradas; A: 4 RPCs muestra cerradas + vista payments_safe + 8 policies + 5 pgsec con status; M: 7 tablas infra lockdown + 24 policies + trigger retención; bajos: event trigger vivo + 0 policies JWT-rol + 0 overloads + 0 TRUNCATE residual + 0 SECDEF sin search_path) → **ledger prod = 262 = repo** · **deploy `1e9d54c0..eec5534f` → los 4 servicios live en `eec5534f`** (connector requirió deploy manual vía API: Render lo saltó por rootDir sin cambios — mismo binario, riesgo cero) · **health ×5 verde** (api `/health` reporta version `eec5534f` + `/health/ready` 200 con DB migrada, connector, orchestrator, web `/login`) · **verificación de seguridad post-deploy en prod: RPCs de infra devuelven `42501 permission denied` LIMPIO vía PostgREST con publishable key** (sin crash — el bug del build local PG 17.6 no existe en prod) · payments_safe expuesta (401 sin auth). **CLI Supabase: intento de bump 2.90.0→2.115.0 REVERTIDO con evidencia** — la 2.115.0 siembra default privileges distintos (MAINTAIN-only a anon/authenticated) que rompen la homologación STG↔PRD (prod cloud = defaults vigentes verificados); el bump de CLI/imagen PG queda como track separado a evaluar contra la versión de prod. **Pendiente founder:** paso 11 smoke pago real mínimo (link prod + confirmación) y paso 12 founder ops (dominio api.konvi.co, M19, desuscripción WABA, anular guía UAT, pin Python 3.13, A1 MFA cuando decida) | `migration list --linked` + Render API + healthchecks + verificación 42501 live (2026-08-22) |
| 2026-08-22 | **TRACK 6 — Wompi ✅** (commit `17605204`): 4 llaves capturables por tenant (pub/integrity opcionales con merge no-destructivo — punto de extensión del checkout embebido Fase 13) · validación de prefijos S0.2 extendida (+5 tests) · copies UI corregidos a la doc oficial (firma SHA256, no HMAC; eventos de token = suscripciones) · dossier `wompi.md` con matriz capacidad×doc×código · datos de prueba sandbox oficiales para UAT B4. Pendiente decisión founder: `taxes` (IVA en checkout — amarrado a B6/DIAN) | Fetch live docs.wompi.co + commit `17605204` |
| 2026-08-22 | **TRACK 6 — Meta WhatsApp ✅** (commit `03ad065f`): enums template HSM vigentes (+7 estados, CHECK constraint migración `20260822130000`, SENDABLE={APPROVED,REINSTATED,UNARCHIVED}) · health check sin campo deprecado · webhook fields nuevos con persistencia (template_category_update→category · user_preferences stop→consent_comercial_revoked_at, resume NO muta · account_alerts→tenant_provider_health) · mark-read+typing al reclamar inbound (worker) · **deadline OPS 2026-09-30 documentado** (método de pago por WABA o se cortan service messages) | Fetch live developers.facebook.com + migración aplicada STG + commit `03ad065f` |
| 2026-08-22 | **TRACK 6 — Supabase ✅** (commit `e7bb52e7`): Realtime select de columnas + anti-truncamiento (re-fetch por id) en hooks inbox · fallback legacy ANON_KEY eliminado · **runbook DR vault root key en HANDOFF §6** (portar vía Management API en restores a proyecto nuevo) · dossier nuevo `supabase.md` · test cross-tenant payments_safe. Pendientes ops [F]: secret key por servicio (4 sb_secret_) + cerrar ciclo JWT signing keys | Fetch live supabase.com/docs + commit `e7bb52e7` |
| 2026-08-22 | **TRACK 6 — Gemini ✅** (commit `80c980a9`): telemetría de uso desagregada (prompt/cached/thoughts → `agentic_shadow_log`, migración `20260822130100`, insert degrade-safe) — fase 0 para medir si flash-lite hace implicit caching (la doc NO lo garantiza: decisión correcta = medir, no asumir) · SDK google-genai 2.11→2.19 · tool_config VALIDATED tras flag `AGENTIC_TOOL_VALIDATED_ENABLED` (constrained decoding ataca la causa de MALFORMED_FUNCTION_CALL) · correcciones doc-código (EOL 3.1-flash-lite 2027-05-07 calendarizado, precios reales 3.x, embeddings) · dossier `gemini.md` | Fetch live ai.google.dev + suite verde con SDK nuevo + commit `80c980a9` |
| 2026-08-22 | **TRACK 6 — Resend ✅** (senders `e03b46d5` + webhook `3d41521c`): senders con tags tenant/order/template + User-Agent explícito (Resend exige uno: 403/1010) + text/plain + reply_to al tenant + log de cuota · **migración `20260822130200` tabla `email_events`** (svix_id UNIQUE dedup at-least-once, RLS tenant, REVOKE a clientes patrón Track 9, GRANT service_role) · **router `POST /api/v1/webhooks/resend`**: firma svix (lib oficial `svix==2.0.0` — ojo: `Webhook.verify` retorna None en éxito), rate-limit fail-open, persistencia ANTES del 200 (durabilidad W2), dedup, routing por tags (data.tags es `Record<string,string>`, NO el array del send), correlación suppression vía `source_id`→`email_id` (suppression.* NO trae tags — verificado), alerta Telegram operador en bounced/complained/failed/suppressed · **suppression list local**: último suppression.added/removed por `occurred_at` (la entrega NO garantiza orden) excluye direcciones en TODOS los senders (ahorra cuota y falsos "enviado") · doc maestro `resend.md` · **E2E STG: firma válida→200+persist+alerta real al grupo · re-entrega→duplicate · firma inválida→401 · suppression.added→exclusión verificada en el sender** · 16 pytest + 4 dbharness. **Pendiente [F]:** registrar el webhook en el dashboard Resend STG+PRD (la key STG es sending_access — no gestiona webhooks: 401 `restricted_api_key` verificado) y setear `RESEND_WEBHOOK_SECRET` en Render | Fetch live resend.com/docs (eventos, payloads, retries 5s→10h, at-least-once) + E2E live STG 2026-08-22 + commit `3d41521c` |
| 2026-08-22 | **TRACK 6 — Telegram ✅** (commit `56f3e31e`): **parse_mode HTML con `html.escape` en los 3 senders** (elimina el fallback "can't parse entities" del Markdown legacy — la causa era contenido del cliente sin escapar) · **inline keyboard "✅ Resolver" en la alerta de takeover** (callback_data `resolve:{conv_id}` 44 bytes ≤64 oficial) + persistencia del message_id en **`telegram_alert_messages`** (migración `20260822130300`: RLS + REVOKE Track 9 + UNIQUE(chat_id,message_id) anti-duplicados pgmq) · **handler `callback_query`** en el webhook (antes se descartaba en silencio): mismo RBAC chat_id→tenant + `answerCallbackQuery` obligatorio (≤200 chars) + **cierre cross-canal de alertas** (`editMessageReplyMarkup` sin reply_markup = quita el teclado) desde callback, `/resolver` y consola (`update_conversation_status`) — anti doble-click entre operadores · **`POST /api/v1/integrations/telegram/setup` [owner/manager] CIERRA M17**: getMe→setWebhook (secret_token + allowed_updates=["message","callback_query"] + drop_pending_updates) → setMyCommands → getWebhookInfo — **verificado LIVE contra `@konvi_stg_bot`** (webhook ngrok registrado, 3 comandos, last_error vacío) · **E2E STG: callback firmado → 200 + bot_active restaurado + alerta cerrada** · puntos de extensión documentados con gates (mensajes efímeros Bot API 10.2 — PII de /estado solo al que pregunta · message_thread_id grupos-foro · secret per-tenant) · 19 pytest + 5 dbharness | Fetch live core.telegram.org/bots/api (Bot API 10.2) + E2E live STG 2026-08-22 + commit `56f3e31e` |
| 2026-08-22 | **TRACK 6 CERRADO (6/6 — MeLi condicionada a S6, documentada como tal):** certificación del cierre — **suite 4906 passed/0 failed · dbharness 316 · vitest 363 · ruff 196 = baseline · `certify_stg.sh` 18/18 · `validate.sh --ci` 24/24 exit 0** · replays de migraciones con baseline regenerado ×2 + STG restaurado y verificado operativo. **INCIDENTE STG vault (resuelto, lección codificada):** el backup de secretos del proceso de restore leía la columna `secret` (ciphertext crudo) de `vault.decrypted_secrets` en vez de `decrypted_secret` → los restores sucesivos doble-cifraban los 6 secretos del tenant STG EN SILENCIO (la verificación `count(*)` pasaba; se descubrió solo al usar la vault contra un tercero real — Telegram getMe con blob). Recuperación: las filas gen0 vivían en el dump completo pre-reset con id+nonce → descifrados los 6 con la key actual (que NUNCA se perdió) y re-sembrados con verificación POR FORMA. Fixes de proceso: `scratch/track9_backup_secrets.py` (backup correcto + guarda anti-ciphertext) + verificación por forma y re-sync de secuencias serial en `track9_restore_stg.py` (GoTrue fallaba login con 23505 refresh_tokens). **Las 4 migraciones Track 6 (`20260822130000` Meta · `…130100` Gemini · `…130200` Resend · `…130300` Telegram) van a PRD por protocolo en el próximo deploy autorizado** | Suite + certify + validate (2026-08-22); recuperación vault verificada (6/6 formas correctas: prv_test_/test_/EAA…/hex32/bot46/demo15) |
| 2026-08-22 | **B-1 CERRADA — calidad conversacional del bot (la queja original del founder; auditoría 2026-08-21, 8 frentes):** (a) **Memoria rodante** — `conversations.conversation_summary` (migración `20260822130400`) generada post-turn con histeresis (>ventana 25 + ≥10 nuevos) por flash-lite instruido a OMITIR montos (la verdad de dinero sale del carrito, no del resumen) e inyectada como primer content de la ventana Gemini — NUNCA en el system prompt (prefijo de caching intacto); el retry por empty_output la preserva (antes cortaba a 5 = amnesia total). **Verificado en vivo: el bot recordó el sérum de 15ml fuera de la ventana de 25** (commit `31304b7f`). (b) **Routing de modelo por estado** tras flag `AGENTIC_STATE_ROUTING_ENABLED` (default off): pre-cart=lite, checkout/carrito=flash con fallback al lite (nunca al modelo caro) + telemetría `model_used` por turno (migración `20260822130500` + `AGENTIC_TRACE`) — canary STG antes de decidir default (commit `a2069b30`). (c) **F5 gate de pago NO destructivo:** cotización sin método → la pregunta se ADJUNTA (antes pisaba el contenido ×2); acción de dinero → rewrite duro (commit `34e786d0`). (d) **F4 afirmación→recotización determinística:** "sí por favor" a "¿te recotizo?" reutiliza el bypass de shipping (adiós loop ×2 idéntico); en vivo además la auto-recotización de ADR-0026 recalculó sin preguntar. (e) **F3 cupón con código:** el prompt exige decir SIEMPRE el código + fallback bare-code ("KAIU15" a secas se aplica — en vivo: -$12.750 sobre $85.000 = 15% exacto). (f) **F6 preguntas mid-flow:** cupones de vuelta en el prompt de estados transaccionales + regla universal "pregunta primero, guion después". (g) **F7 convivencia bot↔operador:** invariant PostEscalationCoherence (escalación con CTA → despedida limpia), race gate (operador habla mid-turn → outbound descartado), ventana de cortesía post-resolve con defer seguro (sweeper re-encola, el mensaje nunca muere). (h) **F8 salida de takeover:** SLA re-alerta 24h (antes silencio eterno), respuesta del operador solo cuenta tras el último inbound, auto-exit de escaladas TÉCNICAS abandonadas (4h), métrica de skips en /agentic/metrics (commit `cadc8a3e`). (i) **C5 calidad de prompt:** regla "máx 4 líneas" con excepción del resumen estructurado (el modelo cortaba el total) + few-shots de objeciones (está caro / lo pienso / llega mal — verificado en vivo "te entiendo + valor + cupón real CON código") (commit `a2069b30`). **Hallazgos del E2E real espontáneo (no harness):** regex del gate de pago no cubría "generar el link" en infinitivo (ampliado), `wipe_conversation.py` moría sin `.env` (fallback `.env.local` — el reset del harness estaba roto y el takeover se tragaba los escenarios siguientes), seed UAT sin pesos (re-siembra + reparación) (commit `69705816`). **Certificación:** suite 4.974 passed/0 failed · dbharness 316 · ruff 196 = baseline · certify_stg 18/18 · validate --ci 24/24 · **E2E conversación real espontánea turno a turno verificada por el founder** (cupón/gate/objeción/recotización/memoria en vivo) · harness coherencia 12/15 (los 3 restantes son aislamiento, no regresión). Las 2 migraciones B-1 van a PRD por protocolo en el próximo deploy | Suite + E2E real + certify + validate (2026-08-22) |
| 2026-08-23/24 | **B-3 CERRADO — harness de evaluación serio + Track 8 adversarial (la red de seguridad del reorden plataforma→bot):** (1) **Assertions de outcome en DB OBLIGATORIAS** — `coherence_assertions.py` reescrito a `TurnCtx` (texto + cart+items + orden+items + pagos + conversación + contacto); la familia nueva verifica la verdad en DB, no el texto: `check_cart_lines`/`check_order_lines` (mata la clase 2→1→3 del audit), `check_order_total_exact` (total recomputado desde ítems−descuento+envío), `check_text_total_matches_order`, `check_payment_link_matches_order`, `check_real_escalation` (human_takeover real en DB, no regex), `check_no_fake_payment_confirmation` ("ya pagué" falso), anti-corchado ×2, `check_cart_discount_exact`, `check_greets_back`. El runner RECHAZA escenarios sin assertions (los 7/15 con cero assertions del audit ahora las tienen; escape auditable `assertion_free_reason`). (2) **Aislamiento fail-closed**: el reset verifica en DB que no quede NADA y ABORTA el run entero (exit 2) — y ya atrapó dos huecos reales de `wipe_conversation.py`: contact sin conversación nunca se purgaba (early-return con "Nada que hacer") y el purge se BLOQUEABA con link Wompi vivo (TTL) justo tras escenarios de dinero → nuevo `--purge-contact --hard` (borrado físico testing-only, sin retención legal ni guard Wompi — datos sintéticos) + wipe ahora falla EN VOZ ALTA (exit 1) ante errores (antes rc=0 con error en stderr = "muere en silencio"). (3) **Bug del propio harness corregido**: las filas `escalation_audit` se contaban como respuesta del bot (falso "escalación silenciosa" en s17) → solo `content_type='text'` cuenta como outbound. (4) **28 escenarios** (18 originales re-armados con outcome DB + 10 Track 8 adversariales `t8_*`: grosero, corchar/descuento inventado, inyección, PII ajena, lenguaje roto, urgencia, cambio abrupto, multi-intención, arrepentimiento mid-checkout, "ya pagué" falso) + `greeting_with_intent` (regresión del saludo, founder 2026-08-23) + `money_full_flow` (LA certificación de dinero: orden creada + líneas exactas + total EXACTO recomputado desde DB + link=orden + status pending_payment — verificado live $35.300 = $24.000 − $3.600 KAIU15 + $14.900 envío). (5) **CI nocturno** `.github/workflows/harness-nightly.yml` (cron 02:23 Bogotá, STG efímero en el runner: supabase start + seeds + 3 servicios homologados PRD vía dev_env_for_service; requiere 4 secrets GH [F]: HARNESS_GEMINI_API_KEY · WOMPI_*_SANDBOX · AVEONLINE_DEMO_PASSWORD). (6) **Corpus dorado** (`scripts/uat/golden_corpus/`: PRD 4d608efd extraída read-only y anonimizada + E2E real 2026-08-23) + **LLM-judge** (`llm_judge.py`: rúbrica coherencia/verdad-dinero/no-alucinación/tono, fail-closed, modo offline, exit 1 si verdad-dinero=0) + **métricas SQL** (`agentic_quality_metrics.py` sobre agentic_shadow_log: distribución model_used, ratio cached/prompt, p95, bloqueos por invariant — para la decisión canary de routing por estado). (7) **E2E real espontáneo de verificación** (patrón founder, `runs/bot_e2e_stg_2026-08-23.md`): 20 turnos, dinero EXACTO en las capas ($109.090 texto=orden=link), defensas adversariales verificadas live (falso pago → orden sigue pending_payment; inyección y descuento inventado rechazados; PII ajena negada; grosería → escalación real). **Hallazgos codificados como xfails permanentes (deuda del bot — bloque bot al final):** H1 narración multi-intención parcial · H3 "la más barata" no selecciona carrier · H5 línea stale del gate post-link · H6 cantidades multi-producto 2→1 en el 1er turno · H8 cambio de tema abandona el carrito. **Fix ARQUITECTÓNICO founder (saludo):** la cortesía de primer contacto vive en el embudo `OutputValidator` (invariant `time-aware-greeting` + rama cold-open con `customer_name` en ValidationContext) — NO parche por callsite (el intento inicial en dispatcher fue retirado); regla permanente: la política transversal de lo que el cliente VE vive en el embudo, los paths producen contenido. **Varianza LLM medida entre corridas** (s10 email-update y s19 escalación post-triage intermitentes) → diseño anti-varianza del harness: turnos de conducción + assertions SOLO en el punto de convergencia. Suite 4.707 pytest (316 dbharness) + 363 vitest + ruff ≤ baseline | Run final `scripts/uat/runs/b3_harness_final_2026-08-24.log` + E2E `runs/bot_e2e_stg_2026-08-23.md` + 34 tests assertions + 26 tests judge/metrics |
| 2026-08-24 | **REORDEN founder — la plataforma primero, el BLOQUE BOT AL FINAL (inclusive su GUI/API/métricas):** el bot es el mayor CONSUMIDOR de las superficies de dominio (inventario, reclamos, órdenes, pagos, catálogo, API); re-ingenierizarlo mientras esas superficies se mueven es construir sobre blanco en movimiento. Nuevo orden (`docs/PLAN-CIERRE.md` §Orden, reescrito sin acumular): Track 5 M1-M5 (dominios modulares — M3 genera las tools del bot del contrato, la razón estructural del reorden) → Track 7 (UX/UI consola) → Track 3 (infra PRD) → remanentes Track 1/2 [F] → Track 4 ops → **AL FINAL el bloque bot**: inventario de inclusiones parche (brief preservado en `.audit/findings/2026-08-23-patch-inventory-brief.md` — los agentes explore cayeron por cuota, queda como entrada del bloque) → B-2 dispatcher sobre contratos estables (resuelve xfails H1-H8) → B-4 observabilidad/métricas + decisión canary con telemetría acumulada → bot GUI/API en consola. **Matiz documentado por objeción founder ("¿el harness no es tema bot?"):** framework desacoplado (persiste intacto y ES el instrumento de aceptación de B-2 — sin él la fase plataforma deja al bot sin certificación continua) + capa de escenarios acoplada al bot actual (se revisa en B-2 — no es rehacer el ejercicio) | `docs/PLAN-CIERRE.md` §Orden + `.context/01-state.md` |
| 2026-08-24 | **TRACK 5 M1 CERRADO + M2 DISEÑO PROPUESTO (paso 1 del §Orden — plataforma primero):** **(M1)** inventario verificable de capacidades de los 11 dominios → `docs/architecture/domain-capabilities-inventory.md` (6 frentes explore en paralelo, cero suposiciones, ~120 citas archivo:línea; las 10 citas más críticas re-verificadas manualmente: sys.path hack de cupones, `aveonline_client.py` ×2 idéntico por diff, ausencia de `GET /orders/`, vocabulario extinto `in_progress`/`closed` vivo en `status_human` del bot, TTL Wompi espejado, ajuste de stock no transaccional declarado en `page.tsx:487-489`). Matriz dominio × (consola REST / consola DB-directo / bot / lógica única) + 6 hallazgos estructurales (H1 compartir=copiar por rootDirs separados · H2 consola bypassea API en lecturas · H3 domain services enterrados en el orchestrator · H4 cimiento adoptable — RPCs SQL, motor cupones ADR-0015, creación de orden unificada REST · H5 RPCs métricas incompatibles con bot por `auth.jwt()` · H6 entidades faltantes: `data_subject_requests`, separación cotización/envío, `rma_requests` muerta) + backlog priorizado de 11 domain services. **Drift medido:** cancelación de pedido ×2 (consola "a medias" sin void/audit vs bot pipeline legal completo) · claims 2 writers divergentes (reason cerrado vs libre; dedup solo en bot) · cliente Aveonline ×2 (1507 líneas) · cotización 2 semánticas (COD solo bot) · restock al cancelar ×3 · consent Habeas Data ×4 máquinas · ingreso reconocido ×5 · P&L en frontend TS con cap 1000 filas · 20 tools del bot, todas customer-facing. **(M2)** contrato de domain services propuesto → `docs/architecture/domain-services-contract.md`: `DomainCapability` declarativa (operaciones con precondiciones, RBAC por `Actor{channel,role}`, idempotencia obligatoria, eventos de dominio sobre la infra existente, errores tipados) + packaging `packages/shared-py/` (mata la copia física y el sys.path hack — deuda ya declarada en `orch/lib/coupons.py:7-9`) + delegación en RPCs SQL + REST como proyección + plan strangler M2.0-M2.4 con tests de paridad canal↔canal + coexistencia bot-congelado defendida por tests de paridad (el bot NO se toca — diff cero en dispatcher/prompts/resolvers/invariants) + 7 criterios de aceptación verificables. **4 preguntas abiertas founder (§8) bloquean M2.1** — packaging shared-py · void Wompi automático desde consola · reason de reclamos (cerrado+detalle vs libre) · lecturas consola→REST — **SIN código de producción hasta el visto bueno**. Cierre del paso: suite **4707 passed/23 skipped** · dbharness **316** · `certify_stg.sh` **18/18** · `validate.sh --ci` **24/24** (ruff 195 ≤ baseline) | Docs de arquitectura + suite/certify/validate verdes (2026-08-24); commit de esta sesión |
| 2026-08-25 | **TRACK 5 — M2 DISEÑO APROBADO por founder (4/4 en la opción recomendada) + M2.0 CERRADO (cimientos del paquete de dominio, sin cambio de comportamiento):** founder aprobó las 4 decisiones del contrato (`domain-services-contract.md` §8): packaging `packages/shared-py/` · cancelación desde consola con pipeline legal completo (void Wompi + cancel guía + audit, tras MFA AAL2) · reason de reclamos = vocabulario cerrado + `reason_detail` libre · lecturas consola → REST dominio a dominio (pilotos primero). **M2.0 ejecutado:** nuevo paquete `packages/shared-py/` (`konvi_domain` 0.1.0) — el motor de cupones ADR-0015 se extrae como **ÚNICA FUENTE** (los `lib/coupons.py` de api y orchestrator quedan como shims re-exportadores con identidad de objetos verificada; **el sys.path hack murió** — deuda declarada en `orch/lib/coupons.py:7-9`) + primitivas del contrato (`Actor{channel,role,tenant_id}`, `DomainError` con códigos estables, `DomainEvent` sobre la infra existente, sin bus nuevo). **Wiring verificado empíricamente:** pip resuelve `-e` relativo al CWD (NO al requirements.txt — probado en /tmp) → buildCommand Render ×2 (`-e ../../packages/shared-py` desde rootDir) + paso explícito en CI ×2 jobs y en `harness-nightly.yml` (si no, la red B-3 rompía esa misma noche) + cache-dependency-path ×3 + `make -C .local deps` con guard fail-closed en `_check`. **Guard nuevo `tests/test_konvi_domain_pact.py`** (4 tests/18 subtests): identidad shim↔paquete · anti-reaparición del sys.path hack · API canónica expuesta · import sin efectos colaterales (subproceso limpio). **2 fallos del propio cambio atrapados por el gate y corregidos:** (1) el pact test fallaba SOLO bajo xdist — execnet no serializa `PosixPath` en parámetros de `subTest` → `str()` (lección: subTest params siempre primitivos); (2) tsc falló por escritura entrecortada de `.next/dev/types/validator.ts` durante el restart del dev server (artefacto local, regenerado — no código). Cierre: suite **4724 passed** (xdist+SLOW) / serial 4711+4 · dbharness **316** · `certify_stg.sh` **18/18** (stack reiniciado con el código nuevo) · `validate.sh --ci` **24/24** exit 0 (coverage 73.5%, ruff 194 ≤ baseline) | Commits de esta sesión; log `/tmp/validate_m20_v3.log`; wiring en `render.yaml`, `ci.yml`, `harness-nightly.yml`, `.local/Makefile` |
| 2026-08-25 | **TRACK 5 M2.1 CERRADO — OrdersService en la capa de dominio (primer piloto del contrato aprobado 2026-08-25):** `konvi_domain.orders` (models + service + `ORDERS_CONTRACT` declarativo — 8 operaciones declaradas, 4 implementadas) extraído INTACTO de `routers/orders.py` (misma secuencia de queries, mismos mensajes/códigos): **create** (total recomputado + herencia de cupón vivo F1 + FK anti-IDOR F27 + adopt-winner 23505 B1 + estados credit/cod Rev.108) · **get** · **list NUEVO — `GET /api/v1/orders/`** (cierra el hueco M1 §H2: filtros estado/contacto/búsqueda D7 + paginación + conteos por estado) · **list_by_contact** (hueco del bot para M3). Router = adaptador delgado (dual-auth/RBAC/MFA/Idempotency-Key/audit + mapeo único DomainError→HTTP); la máquina de estados queda como alias al paquete (fuente única). Efecto de stock al confirmar INYECTADO (`on_confirm_stock`) — sigue en el API acoplado a `sync_meli_stock` hasta InventoryService (M1 #3, decisión documentada). Consola orders migra su listado a REST (Bearer + `no-store` + timeout 15s; dropdowns de otros dominios siguen directos hasta su turno). **Tests nuevos (25):** 16 unit del servicio · 5 estructurales del contrato (§7.4: escritura exige rbac+idempotencia+eventos+errores; `implemented` exige `service_fn` real; import sin efectos colaterales) · 4 de **paridad canal↔canal** (§7.2: misma huella de dominio en DB + mismo resultado vía REST y vía servicio in-process; bookkeeping del adaptador — `audit_log`/`idempotency_keys` — correctamente fuera de la huella de dominio) · pact single-source (4). **Verificación live STG:** list con conteos reales (all/confirmed=1) · 422 status inválido · 401 sin auth · create (total **$110.000 exacto**) · detalle · replay Idempotency-Key (mismo id + header `Idempotency-Replayed`, 0 duplicados en DB) · **harness B-3 `money_full_flow` PASÓ** (el bot creó la orden vía el path extraído: total exacto 35.300, líneas exactas, link=orden — el bot sigue verde sobre la plataforma movida). **Incidentes de entorno (no código):** caída del stack local a mitad de sesión (Kong/auth exited → `make db`+`make up`) y `validator.ts` entrecortado por el dev server mid-regeneración (tsc) → protocolo: `validate --ci` con web detenido o asentado; CI inmune (clone fresco sin `.next`). Cierre: suite **4749 passed** (xdist+SLOW) / 4736 serial · dbharness **316** · `certify_stg.sh` **18/18** · `validate.sh --ci` **24/24 exit 0** (coverage 73.6%, ruff 198 ≤ baseline) | Commits de esta sesión; logs `/tmp/validate_m21_v2.log` + `/tmp/harness_money_m21.log` |
| 2026-08-25 | **TRACK 5 M2.2 CERRADO — cancelación unificada (la consola deja de cancelar "a medias" — decisión founder #2):** pipeline legal Ley 1480 extraído intacto de `lib/order_cancellation.py` → `konvi_domain.orders.cancellation` con **puertos inyectados** (`CancellationPorts`: void_credentials/void_payment/cancel_shipping_guide/on_stock_restored) — cada canal cablea sus efectos de proveedor; el bot conserva su copia congelada (la adopta en B-2). Consola: `PATCH status=cancelled` ejecuta el pipeline completo (triage + audit SIC `order_cancellations` + void Wompi en ventana + cancel guía Aveonline + restock idempotente reservas+movements + WhatsApp cliente + Telegram operador si refund manual) — MISMA superficie, sin endpoint nuevo; RBAC owner/manager + MFA AAL2 heredados. Regla de canal deliberada: la escalación bloquea SOLO a `actor=customer` (el bot autónomo difiere a humano); staff procede y la señal queda en la auditoría. `konvi_domain.legal` = única fuente de plazos legales (antes escritos a mano ×4). `_restore_stock_on_cancel` del router eliminado (1 de las 3 copias del M1 muere; su sync MeLi se preserva vía puerto `on_stock_restored` — el bot lo gana en B-2). **Paridad de outcome certificada** (`test_cancellation_outcome_parity.py`, 11 escenarios: misma huella DB — inserts/updates/RPCs — y mismos resultados; la duplicación time-boxed del bot tiene alarma) + adaptador 7 tests (RBAC/404/409/pipeline/idempotencia/triage staff registrada). **Bug destapado por la certificación LIVE STG (no por los tests):** actor consola "owner" rompe el enum DB `order_cancellation_actor` (22P02 — los try/except del pipeline lo tragaban: la orden NO quedaba cancelled ni auditada pese al 200) → fix `actor="operator"` (valor del enum; granularidad owner/manager en `cancelled_by_user_id` + `audit_log`) + documentado en el paquete. **Live STG ciclo completo:** create+auto_confirm (stock 18→16, movement `sale`) → PATCH cancel (stock vuelve a 18, movement `cancellation_refund` sin duplicar por el guard RPC, audit `completed`/`operator_console`, respuesta con resumen) + re-cancel idempotente sin efectos. Harness B-3: `money_full_flow` + `s11_cancela_preconfirmacion` PASADOS (bot verde sobre la plataforma movida). Gate ruff extendido: `packages/shared-py/` con **cero deuda permitida** (+fix del quirk `grep -c \|\| echo 0` que duplicaba el conteo cuando no hay errores). Cierre: suite **4767 passed** (xdist+SLOW) / 4754 serial · dbharness **316** · `certify_stg.sh` **18/18** · `validate.sh --ci` **25/25 exit 0** (coverage 73.6%, ruff 198 ≤ baseline + 0 en paquete) | Commits de esta sesión; logs `/tmp/validate_m22_v2.log` + `/tmp/harness_money_m22.log` + `/tmp/harness_s11_m22.log` |
| 2026-08-25 | **TRACK 5 M2.3 CERRADO — payment link colapsado (UNA política reuso/TTL; mata el espejo router↔bot medido en M1 §3.3):** `payments.get_or_create_link` extraído intacto a `konvi_domain.orders.payments` — TTL fail-safe leído del env en cada llamada (única fuente; el shim `integrations/wompi_client` lo re-exporta y `wompi_webhook` sigue cableado) · `find_reusable_payment_link` con el criterio EXACTO (eq tenant/order/status=pending + gte created_at cutoff + más reciente + solo si checkout_url no vacío; lookup fallido → degrada a crear) · `validate_link_amount` (round NO int — BLOQUE A — + mínimo $1.500 con mensaje exacto; la rama de reuso salta el guard) · `get_or_create_payment_link` async con el orden de pasos heredado (creds→orden→status→reuso→monto→crear→insert payments→flip pending_payment) y evento `payment.link_created`. **Puertos inyectados** (`PaymentLinkPorts`: wompi_credentials/create_link con **lazy import en call time** — preserva el `patch.object` de los tests del endpoint; `max_attempts=2` queda en el puerto API = presupuesto de latencia del canal, F105). Router = adaptador puro (dual-auth/RBAC/MFA AAL2/rate-limit/audit/Idempotency-Key intactos; finalize 200 en ambas ramas); `_payment_link_expires_at` del router muere (vive en el paquete). **`DomainError.http_status` opcional nuevo** — el "Wompi no configurada" es 503 heredado y no cabía en el mapeo UPSTREAM→500; el adaptador lo honra cuando viene. Contrato: `payments.get_or_create_link` → `implemented=True` (6/8 ops del piloto implementadas). **Tests nuevos (21):** 15 unit del servicio (`test_konvi_domain_payments.py`) · 6 de **paridad de política bot↔paquete** (`test_payment_link_policy_parity.py`: TTL con env limpio — alarma de drift si alguien setea `WOMPI_PAYMENT_LINK_TTL_MINUTES` — + decisión de reuso en 4 escenarios staged + mismos filtros de query) · `test_payment_link_ttl.py` reescrito deliberadamente (identidad shim↔paquete + webhook cableado + alarma TTL bot) · estructural del contrato actualizado. Los 13 tests del endpoint preexistentes pasan SIN tocarlos (compat total). **Lección xdist nueva (fallo real destapado y corregido):** import test→test de un módulo con side effects de colección (`sys.path.insert`) adelantó el binding de `integrations.wompi_client` a antes de la purga `_purge_foreign_integrations` de otro archivo → binding huérfano, patch inútil, 4 tests rojos SOLO bajo xdist (serial verde) — hazard latente pre-existente (reproducido con el código viejo vía stash). Fix arquitectónico: factory compartida movida a `tests/helpers/supabase_mocks.py` (módulo sin side effects); lección registrada en 04-next-steps. **Live STG:** reuso vía REST verificado contra Wompi sandbox real (create → link `checkout.wompi.co/l/test_*`; re-llamada → MISMO checkout_url, 1 sola fila payments, orden pending_payment; cleanup cancelado) + harness B-3 `money_full_flow` (el bot entrega el mismo link en turnos consecutivos — reuso live por el path nuevo) + `s11_cancela_preconfirmacion` PASADOS. Cierre: suite **4776 passed** (xdist) · dbharness **316** · `validate.sh --ci` **25/25 exit 0** (ruff 197 ≤ baseline 202 · 0 en paquete) · `certify_stg.sh` **18/18** | Commits de esta sesión; verificación live `scratch/m23_live_verify.py` (env_guard dev-safe) |
| 2026-08-25 | **TRACK 5 M2.4 CERRADO — ClaimsService completo (7 ops) — el piloto reclamos CIERRA M2 (decisiones founder #3 reason cerrado+detalle, #4 lecturas consola→REST):** writer UNIFICADO `konvi_domain.claims` — `create_claim` (reason de vocabulario cerrado + `reason_detail` libre opcional + **dedup heredada del bot** — hoy solo la tenía él; la consola la gana — + titularidad por actor: `customer` solo sobre pedidos suyos (eq contact_id, anti-IDOR del bot), staff solo tenant + **unión de eventos**: audit_log del router + `messages.claim_audit` con el conversation_id de la ORDEN — se OMITE si el pedido no tiene (MeLi/manual; messages.conversation_id es NOT NULL) — + Telegram operador vía puerto con el texto espejo del bot) · `get_claim` (por id o ticket; scoping customer fail-closed — la defensa P0 del bot contra ticket enumerable) · `list_claims` (embeds PostgREST `orders(id,total_amount,payment_method), contacts(id,name,phone)` + reason_detail — GET /claims/ no tenía consumidores → seguro extenderlo) · `list_claims_by_contact` NUEVO (hueco M1 §3.8; service-only, como orders.list_by_contact en M2.1) · `transition_claim` (FSM formalizada que absorbe PATCH y /resolve: `refunded` FINAL por ambas puertas, `refunded_amount` write-once sellando el KPI net-revenue, reapertura solo owner desde rejected/cancelled, no-op sin re-notificación F-5) · `register_reversion`/`register_reversion_movement` (delegación en las RPCs SECURITY DEFINER — R2, params EXACTOS — con la tabla `_MOTIVO_HTTP` migrada al servicio vía `DomainError.http_status`: 404/409/422 según motivo). Migración `20260825180000` (`claims.reason_detail` TEXT nullable, sin backfill; SIN CHECK de reason a propósito — el bot congelado sigue free-text hasta M3) con protocolo track9 completo (backup `decrypted_secret` → replay → baseline regenerado → restore → **repair del ledger** — lección nueva (a) abajo) → ledger 269 repo / 262 prod (7 pendientes de deploy). Router = adaptador puro (JWT-only — constraint 1: NO dual-auth; RBAC asimétrico G-4 intacto: create owner/manager/operator SIN require_write_role, patch/resolve/reversion con require_write_role + rate-limit + audit decorators; dedup → 200 + `deduplicated:true`, 201 si crea — patrón adopt-winner de orders.create; aliases históricos VALID_STATUSES/etc. → paquete, patrón FSM M2.1). F-5 movida a `lib/claim_ports.py` (mismos textos/lazy import/best-effort; el gate "solo transición real a outcome" vive ahora en `transition_claim`) + `build_api_claim_ports` (Telegram operador + WhatsApp cliente). Consola: `claims/page.tsx` migra su listado a REST (patrón M2.1: Bearer + no-store + timeout 15s; los pedidos del selector siguen directos — lectura del dominio orders) + dialog «Nuevo Reclamo» con textarea `reason_detail` (Kaiu DS) + toast diferenciado de dedup («ya existía»). Contrato: `CLAIMS_CONTRACT` 7/7 ops `implemented=True` (pedidos 8/8 con los 4 no-implem. declarados). **Tests nuevos (38):** 33 unit del servicio (`test_konvi_domain_claims.py`: create/dedup-defensiva/titularidad/unión de eventos/reason_detail trim+500 · get/list/list_by_contact con scoping · FSM completa · reversión con params RPC exactos y motivos→http_status) · 5 de **paridad de política bot↔paquete** (`test_claims_policy_parity.py`: alarma de enums — el `status_human` del bot con vocabulario extinto {in_progress, closed} queda registrado como deuda M3, NO se arregla aquí — + misma decisión de dedup sin insertar + mismas claves de insert compartidas + claim_audit y notificación operador disparados en ambos + **DIFERENCIA deliberada asertada**: bot free-text en `reason` vs paquete cerrado+`reason_detail` + paridad de get por ticket con scoping customer y ajeno → ninguno encuentra) · `test_claim_customer_notification.py` reescrito deliberadamente al puerto (constraint 3 — 6→5 tests, el gate enabled migra al servicio y lo cubre el unit) · estructural del contrato += CLAIMS_CONTRACT + SERVICE_MODULES["claims"] + check de import-sin-efectos extendido. **Heredados verdes sin tocar:** refund_capture ×8 (su fake exige la MISMA secuencia fetch maybe_single→update→res.data[0]) · reversion_api ×13 (params RPC exactos + traducción de motivos + RBAC) · create_rbac ×3 (su fake sin `in_` EXIGE la dedup defensiva — cae al crear, como el bot) · fdoc_postgrest (maybe_single + get_claim direct-call sin `request`) · coherence_pact (fixture regenerado LOCAL — lección (b)) · a3a4_nivel5 (`_validate_status` HTTP legacy del router conservado). **Live STG verificado vía REST con JWT real** (`scratch/m24_live_verify.py`, dev-owner GoTrue local — claims es JWT-only): listado con embeds+reason_detail · create 201 con ticket+detalle · dedup 200 sin duplicar (1 fila claims) · claim_audit en la conversación del pedido · investigating/resolved 200 + **WhatsApp F-5 encolado** (messages outbound text pending) · refunded 422 sin monto / 200 con monto+refunded_at / 409 final · **reversión end-to-end** (radicado RV-000003 presentada → movimiento reembolso_directo → resuelta) · cleanup cancels 200 ×2. **Barra completa:** suite 4826 + dbharness 316 + vitest 363 + ruff 196 (≤202; shared-py 0) + certify_stg 18/18 + validate --ci 25/25 + harness B-3 **`money_full_flow` ✅** y **`s19_reclamo` ✅ intermitente** (flake H11 documentado en el propio escenario + `b3_harness_varianza_2026-08-24.log`: en los fallos el bot nunca sale del triage de pedido — AGENTIC_TRACE muestra SOLO `get_recent_orders` en EXPLORING, nunca create_claim/escalate; la deuda es del LLM triage (bloque bot H11), NO del path de claims ni de este cambio; 1/4 corridas verde, igualando la varianza histórica) + **bot intacto** (diff cero en `agentic/**`). **Lecciones nuevas:** (a) el protocolo track9 de restore NO excluye `supabase_migrations.schema_migrations` → con migración NUEVA el restore recarga el ledger viejo (columna aplicada, versión ausente); fix `supabase migration repair --local --status applied <version>` — o tomar el dump DESPUÉS de aplicar la migración al vivo; (b) `dump_schema_canonical.py` lee `--linked` (PRD) → para columnas pendientes de deploy el fixture se regenera contra la DB LOCAL con el mismo template SQL (hecho: el fixture ganó `reason_detail` + las 7 columnas de las 6 migraciones pendientes de deploy — más fiel al código bajo test). **M2 COMPLETO (pilotos pedidos+reclamos):** sigue M3 (tools del bot generadas del contrato — dentro del BLOQUE BOT, PLAN-CIERRE §Orden). |
| 2026-08-25 | **TRACK 7 INICIADO (T7.1+T7.10 ✅) — la firma diferencial de diseño impregna TODA el área auth (directiva founder 2026-08-25 ampliada: el lenguaje del login va en TODO el front — logout, cambio de contraseña, módulos y submódulos; no solo "llamativo", DIFERENCIAL frente a cualquier desarrollo genérico):** `components/auth/auth-scene.tsx` NUEVO — escena compartida (`AuthScene`: canvas oscuro `.light` forzado INTACTO + grano SVG inline — elimina el asset externo de terceros que mfa sí traía (supply-chain en la puerta del producto) — + aurora ambiental estática primary/amber con blur, solo tokens Kaiu, reduced-motion trivialmente seguro · `AuthBrand`: brand tile degradado primary→amber + `glow-primary` + letterform K — el "Logo mock / Brand" del login muere — con wordmark BLANCO (el `text-gradient` del DS es para superficies claras; en el canvas oscuro el extremo primary no daba AA — el degradado vive en el tile, donde sí resalta) · `AuthCardReveal`: revelado fade+lift 300ms con delay) aplicada a las 4 páginas auth (login, mfa, forgot, set-password) + **`/logout` con despedida de marca** (sidebar repointeado; signOut preserva la limpieza G7 de la cookie `mfa_recovery_session`; beat de marca ≤800ms, best-effort: redirige igual si la action falla; `logoutAction` del layout/sidebar muere). Coreografía vía wrappers del DS (StaggerList/FadeIn — prohibido `motion` crudo, §4.1). **Brief Track 7 derivado con auditoría completa** (`.context/04-next-steps.md` §Track 7, T7.1-T7.12 — hallazgo: gran parte de la Spec WOW §4 ya existía desde `894b7357`: palette ⌘K, drawers, carrusel, StaggerList, EmptyState ×14, boundaries ×7). **Tests nuevos (6):** `auth-scene.test.tsx` ×4 (render, `.light` forzado, grano sin URL externa, logo real K-no-mock) + `logout-farewell.test.tsx` ×2 (action una vez + redirect best-effort). **Barra:** vitest 369 + tsc + lint + suite pytest 4826 + validate --ci 25/25 + live render STG (login 200 con marca/aurora/data-URL, logout 200 con despedida) + certify_stg 18/18 + CI 5/5. UX-UI.md al día (§1.9 inventario real 26 componentes + auth-scene; §4 estado Spec WOW + directiva ampliada). **SIGUE:** T7.2 (motion chat inbox) → T7.3 (pill bottom-nav + layout cards pedidos) → T7.4 (micro-celebraciones dinero) → T7.5 (topbar identidad) → T7.6 (barrido estados) → T7.7 (virtualización) → T7.8 (cobertura render DS) → T7.11 (settings/security con firma) → T7.12 (cabeceras de módulo con identidad). |
| 2026-08-25 | **TRACK 7 · T7.5 ✅ — topbar con identidad de página (la topbar móvil no decía dónde estás — gap §6.2 UX-UI; directiva founder: la firma en TODO el front):** `app/dashboard/topbar-title.tsx` NUEVO — título de ruta resuelto de la fuente ÚNICA `NAV_ITEMS` (la misma del sidebar y de la command palette: cero duplicación de labels/gates) con match por prefijo más largo (detalles tipo `/orders/[id]` heredan su módulo) + overrides de rutas huérfanas (`media` y las 5 de integraciones por proveedor) + contexto de grupo en desktop («Ventas · Pedidos», oculto en móvil donde solo va la página) + `aria-current="page"`. Entra con `FadeIn` keyeado por destino (firma de motion Track 7 vía wrapper del DS — reduced-motion lo neutraliza). Cableada en `layout.tsx` entre el espacio del hamburguesa y la palette (mantiene ⌘K + ThemeToggle + el comentario del indicador "Live" retirado). **Tests nuevos (4):** resolver (hojas directas/agrupadas, prefijo más largo en anidadas, overrides) + smoke del componente con `usePathname` mockeado. **Barra:** vitest 373 + tsc + lint + suite 4826 + validate --ci 25/25 + CI 5/5. SIGUE: T7.2 (motion chat inbox — el punto delicado: keys estables para que el dedupe optimistic/realtime no re-anime). |
| 2026-08-25 | **TRACK 7 · T7.2 ✅ — motion del chat del inbox (donde vive el operador; Spec §4.2):** burbujas NUEVAS entran con slide-up 200ms y NADA más se re-anima — `BubbleIn` + `AnimatePresence` (wrappers del DS, prohibido `motion` crudo) en `chat-panel.tsx` con entrada decidida por `_hooks/use-animatable-messages.ts` NUEVO (id no visto + `created_at >=` max visto: excluye carga inicial, prepend histórico de loadMore y dedupes polling/realtime; `>=` cubre dos inserts en el mismo ms) + **`loadedConvId` en `use-messages`** (guard de stale paint: sin él, cambiar a una conversación más reciente animaba su carga en cascada). `layout` solo en la burbuja nueva; sin `exit` (la UI no borra mensajes). **Fix arquitectónico DS de paso (expuesto por la sonda live):** `useReducedMotionDS` hidratación-seguro en los wrappers de entrada (FadeIn/StaggerList/StaggerItem) — framer lee la media query real en hidratación y el SSR no puede → mismatch pre-existente de T7.1 para usuarios con reduce; ahora SSR/CSR coinciden y el valor real aplica post-mount (cero cambio visual para no-reduced; BubbleIn/Pressable quedan con lectura directa — client-only / sin `initial` SSR). **Tests nuevos (11):** hook ×8 (anti-cascada, dedupe nunca re-anima, mismo-ms, prepend excluido, switch de conversación, conv vacía, null) + BubbleIn ×3. **Barra:** vitest 384 + tsc + lint 0 err + suite 4826 + dbharness 316 + validate --ci 25/25 + certify_stg 18/18 + **verificación visual live con navegador real** (`scratch/t7_02_visual_verify.py` — playwright en venv aislado, capacidad nueva del repo: MutationObserver registra el style inline al montar cada burbuja; inserts/updates SQL disparan realtime): carga inicial 0/40 en cascada · slide-up `opacity:0+translateY(12px)`→asienta `transform:none` · UPDATE realtime 0 re-mounts · reduced-motion fade-only · móvil 390px slide-up + chrome · temas light/dark · **0 errores de consola** (el fix mató el mismatch). Lecciones entorno en 04-next-steps (sonda playwright: `localhost` NO `127.0.0.1` — allowedDevOrigins bloquea /_next y la página no hidrata; observer en DOMContentLoaded; tap tras hidratación). Bot intacto: diff cero en `agentic/**`. SIGUE: T7.3 (pill bottom-nav + layout cards pedidos). |
| 2026-08-25 | **TRACK 7 · T7.3 ✅ — física de navegación y pedidos (Spec §4.2):** `NavPill` NUEVO en el DS (`ui/motion.tsx`) — pill que VIAJA entre destinos del bottom-nav con `layoutId` (250ms, easing DS; estática bajo reduced-motion; `aria-current` y badge de takeover intactos — la pill es solo refuerzo visual, `aria-hidden`) + `LayoutItem` NUEVO — reubicación suave de cards de `orders-manager.tsx` al filtrar/paginar (wrapper de TODA card con la key; el StaggerItem queda dentro para la entrada de las nuevas — sin cascada infinita). **Bug real cazado por la sonda live (fix arquitectónico DS):** `StaggerItem` con `variants={undefined}` bajo reduced-motion NUNCA reseteaba los valores en vuelo → las listas con StaggerList (pedidos, inbox) quedaban **congeladas en `opacity:0` — invisibles para usuarios reduced-motion desde 894b7357**. Fix: `itemVariantsReduced` (mismos valores finales, `transition: duration 0` → snap instantáneo post-hidratación, verificado live: cero elementos congelados). **Tests nuevos (8):** NavPill ×3 + LayoutItem ×2 (motion.test) + bottom-nav ×3 (pill solo en activo, aria-current, badge vivo, reduced estática). **Barra:** vitest 392 + tsc + lint 0 err + suite 4826 + dbharness 316 + validate --ci 25/25 + certify_stg 18/18 + **verificación visual live** (`scratch/t7_03_visual_verify.py`): cards viajan al filtrar (56 mutaciones `translate3d` — lección: framer usa translate3d para layout/layoutId, no translateX/Y) · pill viaja Inbox→Pedidos (16 mutaciones) y respeta aria-current · reduced-motion todo estático · dark/light OK · 0 errores consola/pageerror. Bot intacto (diff cero en `agentic/**`). SIGUE: T7.4 (micro-celebraciones de dinero — verificación live con pago Wompi sandbox STG). |
| 2026-08-25 | **TRACK 7 · T7.4 ✅ — micro-celebraciones de dinero (Spec §4.2, verificado con DINERO SANDBOX REAL):** al llegar por realtime una transición a `confirmed`/`delivered`, UN toast sonner con `CelebrationCheck` (wrapper DS nuevo: pop spring único; estático en reduced-motion) + monto con count-up sutil (`useCountUp`; reduced muestra el final directo) — sin confetti pesado. Detección PRECISA en `app/dashboard/money-celebration.tsx` NUEVO: `orders` tiene REPLICA IDENTITY FULL → `old.status` viaja en el payload UPDATE (una edición de notas sobre un pedido ya confirmado NO celebra) + dedupe por `${orderId}:${status}` de sesión. En el home se engancha al canal orders YA existente (sin segunda suscripción); en el detalle de pedido, isla `OrderStatusLive` NUEVA (`orders/[id]/_components/order-status-live.tsx`) que celebra y hace `router.refresh()` — el detalle ya no necesita F5 para ver el pago confirmado. **Fix DS cazado por la sonda live:** `Pressable` con `whileTap` emite `tabindex="0"` en SSR (framer hace focusable el gesto) → hydration mismatch intermitente bajo reduced-motion; ahora también pasa por `useReducedMotionDS`. **Tests nuevos (11):** transición payload ×6 + dedupe ×2 + render toast ×1 + CelebrationCheck ×2. **Barra:** vitest 403 + tsc + lint 0 err + suite 4826 + dbharness 316 + validate --ci 25/25 + certify_stg 18/18 + **verificación live con dinero sandbox real** (`scratch/t7_04_visual_verify.py`: orden vía REST → payment link Wompi sandbox real → webhook firmado `APPROVED` → `confirmed` → realtime): toast único "Pago confirmado" con monto exacto en home ($2.000) y detalle ($45.000), badge del detalle pasa a "Confirmado" sin F5, reduced-motion con check estático, 0 errores consola/pageerror. Lección registrada (04-next-steps): el webhook script corre con `python3.11` del sistema (lee events_key del Vault); apunta a la última fila `payments` del tenant. Bot intacto (diff cero en `agentic/**`). SIGUE: T7.6 (barrido de estados: ~44 vacíos ad-hoc + error.tsx en rutas de alto tráfico) y T7.11/T7.12 (firma en settings + cabeceras de módulo). |
| 2026-08-25 | **TRACK 7 · T7.6 ✅ — barrido de estados (vacíos/errores/cargas con diseño, anti-falso-0 intacto):** **(a)** `components/route-error.tsx` NUEVO — superficie única de boundary por módulo (card + retry + link inicio + `#digest` + log con tag), extraída de los 6 error.tsx idénticos — y **23 `error.tsx` por ruta** (17 nuevos cubriendo contacts, purchases, finance, claims, team, integrations — cubre los 5 providers —, settings — cubre security/retention/health/legal/account-closure —, marketplace, ai-agents, categories, promotions, receipts, inventory, audit, knowledge-base, media, account + los 6 preexistentes migrados al compartido): un error de página ya no tira la sección dashboard completa (gap §6.4). **(b)** Barrido EmptyState: ~20 sitios ad-hoc en 13 archivos → `EmptyState` del DS (team, categories, media, whatsapp-quality ×2, movimientos del drawer de producto, panel contextual ×2, metrics-charts ×2, metrics ×5, gastos con CTA, chat-panel ×2, las 4 páginas "Sin acceso", notas del inbox) — excluidos a propósito y documentado: micro-notas de dropdowns (layout crítico) y la alerta ámbar de proveedores (guía, no vacío). Gap §6.3 cerrado. **(c)** Skeletons: 14 `loading.tsx` + 4 componentes inbox (lista, contexto, notas, imagen) de divs crudos a `ui/skeleton` (0 `bg-muted` crudos restantes). **Tests nuevos (3):** route-error (render + retry + digest + log tag). **Barra:** vitest 406 + tsc + lint 0 err + suite 4826 + dbharness 316 + validate --ci 25/25 + certify_stg 18/18 + visual live 8/8 (`scratch/t7_06_visual_verify.py`: 27 rutas renderizan sin errores consola/hidratación, media empty light+dark, finance empty con CTA, spot móvil). **Incidente de proceso (auto-infligido, resuelto):** la sonda T7.4 dejaba comprobantes en `order_receipts` y rompía `test_order_receipts` del dbharness (asume tabla prístina; `assert 8 == 1`) — lección registrada: TODA sonda que confirme pedidos borra sus filas al final (la sonda T7.4 ahora se auto-limpia y se re-verificó verde dejando la tabla en 0). Bot intacto (diff cero en `agentic/**`). SIGUE: T7.7 (virtualización audit/conversations) → T7.8 (cobertura render DS) → T7.11/T7.12 (firma en settings + cabeceras de módulo) → BUG-105-02. |
| 2026-08-25 | **TRACK 7 · T7.7 ✅ — virtualización donde paga: NO paga en ninguna superficie (decisión MEDIDA, no asumida) + dep muerta `@tanstack/react-virtual` retirada.** Auditoría del brief contra código + live STG: la premisa "audit = miles de filas" NO se sostiene — `audit/page.tsx` pagina **server-side** a 25/pág (DOM máx 25; verificado live con datos reales STG: 68 eventos → 3 págs, 220 accesos PII → 9 págs, navegación p1→p2 con set distinto); `conversation-list` tiene cursor ventana 50 + 50 por click **deliberado** y agrupación por teléfono con filas expandibles de altura variable (virtualizarla rompería esa semántica por un escenario que exige ~9 clicks seguidos); barrido de TODAS las listas grandes: contactos (ventana 500 + paginación cliente 30/pág), pedidos (REST per_page=20), despachos (50), marketplace (50/pág), compras (100+300), promotions (redenciones on-demand ≤500). Único borde: catálogo (ventana protectora 1000 sin paginación cliente) — trigger documentado: si un tenant real se acerca, el fix consistente es paginación cliente estilo contacts, NO filas virtuales (romperían la tabla expandible con `aria-expanded`/teclado). Decisión completa + tabla de cotas en `docs/ux/UX-UI.md` §4.6 (reescrito) + §4 header; `.context/02-stack.md` al día. `pnpm remove @tanstack/react-virtual` (estaba desde 894b7357 sin un solo import — grep lo confirma). Sin código runtime nuevo → sin tests unitarios nuevos; la evidencia ES la sonda live. **Barra:** vitest 406 + tsc + lint 0 err + validate --ci 25/25 (suite 4826 + dbharness 316 incluidos; osv-scanner limpio tras el cambio de lockfile) + certify_stg 18/18 + **sonda live 17/17** (`scratch/t7_07_visual_verify.py`: cotas de DOM medidas en audit ×2 vistas/inbox/catálogo + ambos temas + móvil 390px + reduced-motion + 0 errores consola/pageerror; screenshots light/dark revisados). Bot intacto (diff cero en `agentic/**`). SIGUE: T7.8 (cobertura render DS: empty-state, carousel, drawer, responsive-dialog, confirm-dialog, skeleton) → T7.11/T7.12 → T7.9 opcional → BUG-105-02. |
| 2026-08-25 | **TRACK 7 · T7.8 ✅ — cobertura de render del DS (gap §6.5 UX-UI):** 20 tests vitest nuevos para los 6 primitivos con lógica propia — `empty-state` ×5 (variantes, icono aria-hidden, slot action, margen condicional) · `skeleton` ×2 · `confirm-dialog` ×5 (flujo promise-based: error claro sin provider, resuelve true/false por botón, variante destructive, labels por defecto Cancelar/Confirmar) · `responsive-dialog` ×2 (ambas ramas ≥lg Dialog / <lg bottom-sheet — stub de `matchMedia` con **getter** porque `use-media-query` cachea el mql por query a nivel módulo) · `drawer` ×3 (portal, superficie tokens + handle aria-hidden + safe-area, cerrado no renderiza) · `carousel` ×3 (roles region/carrusel + group/diapositiva, guard de dots con ≤1 snap, error fuera de contexto). **Lección jsdom:** embla 8.6 exige stub de `IntersectionObserver` + `ResizeObserver` además de `matchMedia` (mismo patrón que `motion.test.tsx`). Sin código runtime nuevo. **Barra:** vitest 406→426 (52 archivos) + tsc + lint 0 err + validate --ci 25/25 (suite 4826 + dbharness 316) + certify_stg 18/18 + **sonda live 19/19** (`scratch/t7_08_visual_verify.py` — los primitivos ejercitados en sus superficies reales sin mutar nada: dots del carrusel del home móvil con exactamente un `aria-current` · ResponsiveDialog bottom-sheet CON handle en pedidos móvil y Dialog SIN handle en desktop, "Volver" cierra sin mutar · ConfirmDialog global en integraciones ("¿Conectar Mercado Libre?" → "Cancelar", sin redirect OAuth) · EmptyState media light/dark · skeletons del inbox visibles con PostgREST demorado 800ms y reemplazados por la conversación real · reduced-motion · 0 errores consola/pageerror). Bot intacto (diff cero en `agentic/**`). **Fix de proceso de paso:** la barra expuso un flake PRE-EXISTENTE de vitest — "Errors 3 errors" intermitente (`ReferenceError: window is not defined` del scheduler de React 19 ejecutando trabajo tras el teardown de jsdom, "originated in route-error.test.tsx"; rompe el exit code con todos los tests verdes, más probable bajo presión de memoria tras tsc/pytest). Causa raíz: RTL NO auto-limpia en este repo (vitest sin `globals: true`) y 6 archivos jsdom no llamaban `cleanup` — barrido: `afterEach(cleanup)` en route-error, topbar-title, auth/callback, logout-farewell, badge, auth-scene. Reproducido (1/3 bajo secuencia tsc+vitest) y verificado extinto (5/5 ciclos limpios). Lección en 04-next-steps. SIGUE: T7.11 (settings/security con la firma — piloto PageHeader) → T7.12 (cabeceras transversales) → T7.9 opcional → BUG-105-02. |
| 2026-08-25 | **TRACK 7 · T7.11 ✅ — Settings/Seguridad con la firma + `PageHeader` NUEVO en el DS (piloto del patrón que T7.12 hará transversal):** `components/ui/page-header.tsx` — cabecera de módulo con identidad: tile degradado primary→amber + `glow-primary` + glifo Lucide blanco (hermano del brand tile de auth — la marca de la casa a escala de módulo) + h1 + contexto + slot de acciones + coreografía de entrada stagger vía wrappers DS (§4.1, hidratación-segura por `useReducedMotionDS`; el componente NO lleva 'use client' — quien importa decide el grafo y el icono nunca cruza la frontera server→client). **Security re-superficiada:** header plano → PageHeader (ShieldCheck + email de la cuenta como contexto) · las 3 superficies (card Contraseña, card MFA, footer) entran con revelado escalonado (StaggerList 0.08) · sección MFA de div plano a `Card` del DS (paridad con Contraseña) · los 4 avisos inline con literales legacy (emerald-50/red-50/amber-50 + borders 200) → variantes semánticas de `Alert` (success con `role="status"` preservado, destructive, warning ×2 — la regla aria-live se respeta por spread de props). Superficies delicadas INTACTAS a propósito: QR con `bg-white` (escaneabilidad) y la card ámbar de recovery codes (superficie documento de una sola vez). **Tests nuevos (5):** page-header (h1 único + tile degradado/glow + glifo blanco aria-hidden, sin description, slot actions, merge className, reduced-motion) — lección: en SVG `className` es SVGAnimatedString, asertar con `getAttribute('class')`. **Barra:** vitest 426→431 + tsc + lint 0 err + validate --ci 25/25 (suite 4826 + dbharness 316) + certify_stg 18/18 + **sonda live 34/34** (`scratch/t7_11_visual_verify.py`: tile + h1 + contexto + ambas cards en light/dark/móvil/reduced-motion, coreografía registrada por MutationObserver (32 mutaciones), alerts success/destructive via query string sin mutar nada, `/account` redirect aterriza en la superficie, reduced-motion sin nada congelado en opacity 0, 0 errores consola/pageerror; screenshots light/dark/móvil revisados). Bot intacto (diff cero en `agentic/**`). SIGUE: T7.12 (PageHeader transversal a los módulos) → T7.9 opcional → BUG-105-02. |
| 2026-08-27 | **TRACK 7 · T7.12 ✅ — cabeceras de módulo transversales con la firma (`PageHeader` en TODOS los módulos — completa la directiva founder "módulos y submódulos"; TRACK 7 queda CERRADO salvo T7.9 opcional + verificación BUG-105-02):** rollout del patrón pilotado en T7.11 ejecutado archivo-por-archivo por el agente principal (NO swarm — lección del intento abortado aplicada: `git status` limpio verificado al inicio). **25 headers estándar** transformados (icono Lucide del propio h1, línea de contexto VERBATIM, acciones a `actions` con condicionales `c ? (x) : undefined`): ventas ×6 (orders-manager —client, search+role badge a actions—, contacts, promotions, receipts —icono nuevo `FileText`—, shipping, claims —la rama `ClaimsError` NO se toca: las ramas de error/EmptyState no llevan firma—) · productos ×3 (products-manager —client, CTA "Nuevo Producto" condicional—, categories-manager —client—, media) · canales/IA ×3 (marketplace, ai-agents —badge guardrails a actions—, knowledge-base) · analítica/finanzas/compras ×4 (audit —Export CSV condicional—, metrics —filtros en Suspense—, finance, purchases) · configuración ×9 (integrations-manager —client—, **`PanelHeader` compartido ×4 providers** —breadcrumb intacto encima, línea Conectado/Desconectado con dots al description verbatim, prop `Icon` tipada `LucideIcon`—, whatsapp, team, settings, health —icono nuevo `Activity` + RefreshButton a actions—, legal, retention, account-closure —icono nuevo `TriangleAlert`). **5 casos especiales:** home (el saludo «Bienvenido, {tenant}» ES el header → `PageHeader.title` ahora **ReactNode** para conservar el `text-gradient`; rtState+alertas a actions) · **inbox = excepción documentada (UX-UI §5)**: el h1 vive en el panel angosto w-80 → mini-tile inline `h-7 w-7 rounded-lg` del MISMO degradado junto a «Inbox AI», chip Live intacto · detalle pedido (back-link intacto; badges+total a actions; cliente+fecha al description) · detalle comprobante (PageHeader con **`print:hidden`** — el CSS de impresión oculta todo `<header>` y el componente renderiza `<div>`s: sin la clase el título aparecería de golpe en el documento impreso; artefacto impreso byte-idéntico) · visor legal (`Scale`). **Bug PRE-EXISTENTE cazado por la sonda (fix arquitectónico):** `legal/view/[doc]` era `force-static` bajo el layout autenticado del dashboard → el layout no recibía las cookies de sesión → `getUser()=null` → redirect 307 a /login: el visor legal era INALCANZABLE (verificado idéntico en baseline con `git stash`; cadena de redirect capturada por la sonda); ahora `force-dynamic` — chain sin redirect verificada live. **Barra:** vitest 431 + tsc + lint 0 err (44 warnings = baseline exacta, cero nuevas) + validate --ci 25/25 (suite 4826 incl. dbharness 316 + next build) + certify_stg 18/18 + **sonda live 17/17** (`scratch/t7_12_visual_verify.py`: barrido de **30 rutas** — las 27 de T7.6 + settings/legal+retention+account-closure — con assert del tile `span[aria-hidden].bg-gradient-to-br` junto al h1 en 27 (+marketplace en rama gate documentada: MeLi no conectado en STG → el EmptyState manda), detalle de pedido real + visor legal con tile, home/orders dark, móvil 390px ×4 (mini-tile inbox visible), reduced-motion con h1 ESTÁTICO opacity 1 desde el primer frame ×3 rutas, 0 errores consola/pageerror; screenshots light/dark/móvil revisados). Sin tests nuevos (cambio presentacional mecánico; `page-header` ya cubierto por los 5 tests T7.11). Bot intacto (diff cero en `agentic/**`). SIGUE: T7.9 opcional + verificación BUG-105-02 → **Track 3** (§Orden paso 3: infra PRD — dominio api.konvi.co + Render Projects, pin Python 3.13, dev cloud, G8b media privada). |
| 2026-08-27 | **BUG-105-02 ✅ CERRADO (obsoleto — verificado live STG):** el inbox SÍ refresca sin F5 desde el refactor 2026-05-29 + Track 6 (realtime `postgres_changes` por conversación en `use-messages.ts` con polling fallback si el canal erra + canal `conversations:all` con select explícito anti-truncamiento y polling 20s en `use-conversations.ts`). Sonda `scratch/bug105_02_verify.py` 6/6: INSERT SQL directo a `messages` → burbuja en el panel en ≤15s SIN recargar ×2 mensajes + preview actualizado en la lista de conversaciones sin F5 + 0 errores consola/hidratación + auto-limpieza de las filas sintéticas. Entrada del tracker (`docs/bugs/known-bugs-tracker.md`) cerrada con la evidencia. |
