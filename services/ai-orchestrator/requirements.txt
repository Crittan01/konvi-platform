google-genai==2.19.0
httpx==0.28.1
PyJWT==2.13.0
pydantic==2.13.4
pydantic-settings==2.15.0
supabase==2.31.0
python-dotenv==1.2.2
fastapi==0.139.0
# Pin explícito (YELLOW-10): ver comentario en services/api/requirements.txt —
# sin pin el lock resuelve starlette 1.x (bump mayor no regresionado).
starlette==0.49.3
uvicorn[standard]==0.51.0
# Rev. 109 P1 #2 — OpenTelemetry mínimo viable. Exporter desactivado por
# defecto (no emite spans en producción a menos que se active explícito).
# Activar con OTEL_EXPORTER_ENABLED=true para debug local.
# Stack libre para Grafana Tempo / Jaeger exporters futuros.
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
