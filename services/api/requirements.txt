fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
# G13 — config central validada en boot (services/api/config.py)
pydantic-settings==2.15.0
supabase==2.31.0
python-dotenv==1.2.2
python-multipart==0.0.32
httpx==0.28.1
PyJWT==2.13.0
# Rev. 72 — embeddings KB en backend (cierra D3: GEMINI_API_KEY ya no expuesta al frontend).
# Track 6 (2026-08-22): 2.19.0 — paridad con ai-orchestrator (el job py-core de CI
# instala api+orchestrator en el mismo env; versiones distintas = ResolutionImpossible).
google-genai==2.19.0
# Rev. 105 Sem 2 F.10 (MA-2) — bcrypt hashing para webhook secrets rotables per-tenant.
bcrypt==5.0.0
# Track 6 (2026-08-22) — verificación de firma svix del webhook Resend
# (routers/resend_webhook.py). Envuelve standardwebhooks (HMAC-SHA256 + timestamp).
svix==2.0.0
